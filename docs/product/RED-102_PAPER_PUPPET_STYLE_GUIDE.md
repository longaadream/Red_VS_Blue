# RED-102 纸偶剧场战斗界面风格指南

## 目标与边界

RED-102 将现有 2.5D 战斗页统一为“手工纸偶 DND 桌面剧场”。角色本体保留各自 IP 的身份锚点，纸张、笔触、裁切边和 UI 材质负责统一风格；红蓝阵营只由独立底座、状态条和交互高亮表达。

本任务只改变表现层。棋盘坐标、规则、行动点/充能点计算、状态字段、地格字段、技能合法性、回合倒计时和 RED-69 动画触发保持不变。

## 跨 IP 统一方法

每名角色必须先保留五类身份锚点，再套统一纸偶材质：

1. 头部轮廓与发型。
2. 面部识别特征。
3. 代表性服装大色块。
4. 一件代表性武器或道具。
5. 角色原本的主色，不叠加阵营色。

统一层使用粗黑毛笔轮廓、可见铅笔底稿、低饱和彩铅/干式套印、暖色纤维卡纸和不均匀米白裁切边。不得用写实 PBR 金属、光滑古典棋子或整洁无瑕的矢量边缘。角色缩到约 120 px 高时，仍应靠轮廓和主色辨认。

## 角色输出规格

| 项目 | 规格 |
| --- | --- |
| 文件 | 透明 PNG，sRGB |
| 画布 | 512 × 1024 px |
| 安全区 | 四周至少 32 px |
| 脚底基线 | y = 932 px |
| 角色占高 | 建议 78%–84% |
| 轮廓线 | 512 px 画布上约 7–10 px，允许自然抖动 |
| 裁切边 | 暖米白 6–12 px，不使用阵营色 |
| 阴影/底座 | 不烘焙进角色图，由运行时独立生成 |
| 命名 | 与 templateId 一致：`public/standees/<templateId>.png` |

透明背景必须是真实 alpha，不能把棋盘格烘焙进 PNG。武器、头发和衣摆不得越过安全区；脚底必须落在统一基线，避免不同 IP 上棋盘后忽高忽低。

## 运行时展示规格

- 角色图使用竖直纸片网格，默认高约 1.08 个地格、宽约 0.56 个地格；尺寸小于旧样板，优先避免密集战局遮挡。
- 角色纸片不接收阵营染色。红蓝识别保留在独立椭圆底座、无色盲依赖的单点/双点标记和合法行动高亮。
- 纸偶加载成功后隐藏旧椭圆头像；缺图、映射缺失或纹理加载失败时立即保留旧头像，不发起猜测文件名的请求。
- 点击和触摸仍按权威地格坐标判定，不以纸片透明像素作为命中区域。
- 角色受击、治疗、死亡、待确认和状态变化仍作用于现有角色根节点。

## 棋盘与 UI

- 桌面是一张与棋盘平行的水平平面；棋盘是一张带轻微撕边、贴在桌面上的薄纸，不显示厚木盒、前沿立面或逐格方块高度。
- 地格线由一组共享的轻微抖动线段画在纸面上。墙、掩体、出生点、弹簧、充能点、陷阱、坑洞和岩浆都使用最少笔画的平面符号，不使用竖立插画或硬 3D 模型。
- 每种地图符号只表达规则类别，不表现复杂物件细节；墙使用砖线、掩体使用折线、陷阱使用叉号等，在同一格内最多保留必要轮廓。
- 旧的 `terrain-wall.png` 与 `terrain-cover.png` 仅保留为提交级回滚资产；当前渲染路径不请求它们。桌面纹理来自 `public/standees/tabletop-wood.svg`。
- 原始地形高度只存在于不可见射线命中代理中；可见纸面与棋子基线保持平行和统一，因此美术变化不修改权威地格坐标。
- 固定镜头继续使用 RED-68 的 35° FOV 与 45° 俯角；本轮只替换可见材质与几何，不改变既有缩放、平移和投影合同。
- 回合、行动点、充能点、状态栏、地格信息栏和点击角色后的行动菜单继续使用现有布局槽位。
- 大标题使用楷体/手写感字体栈，小字与数字使用清晰的宋体或等宽字体栈；不依赖远程字体。
- 按钮必须保留 44 px 触控目标、键盘焦点、禁用态和 `prefers-reduced-motion` 处理。

## 当前素材清单

运行时清单为 `data/pages/js/battle-ui/battle-standee-manifest.js`，管线清单为 `data/pages/public/standees/manifest.json`。两者由自动测试校验一致。

| templateId | 状态 | 说明 |
| --- | --- | --- |
| blue-naruto | ready | RED-102 样板 |
| jaina | ready | RED-102 样板 |
| arthas | ready | RED-102 样板 |
| ana, anduin, blue-kenshin, blue-minato, blue-tirion-fordring, blue-watcher, guldan, hashirama-edo, kiljaedan, liadrin, reaper, red-blackwidow, red-doomsday-fist, red-hidan, red-illidan, red-obito, red-rafaam, red-sasuke, red-shishio, red-venom, tracer, tyrande, uther | portrait-fallback | 继续使用 RED-68 椭圆头像，待后续批量生产 |

Linear 合同写的是 25 个角色；当前 `origin/main` 已新增 `red-venom`，因此本清单按真实代码覆盖 26 个 templateId。RED-102 本轮仍只生产三张样板，不擅自扩大美术确认范围。

## 三张样板来源

三张样板使用内置图像生成工作流分别生成，随后只做两项机械处理：移除生成图中的浅色棋盘格背景、按统一比例缩放为 512 × 1024 并保留 alpha。原始生成文件保留在 Codex 生成目录，项目内使用标准化副本：

- `data/pages/public/standees/blue-naruto.png`
- `data/pages/public/standees/jaina.png`
- `data/pages/public/standees/arthas.png`

三张图进入全量角色生产前必须由项目负责人确认：角色身份是否准确、跨 IP 是否足够统一、纸张和笔触是否够“手工”、小尺寸轮廓是否清楚。

纸张底纹来自现有样板资产；桌面使用低细节循环 SVG。地图符号由 Three.js 线段在纸面上直接构造，不再为墙和掩体加载独立插画，也不需要扩充地形图集。

## 回退与回滚

单个角色回退：把其清单项设为 `portrait-fallback` 并移除 `src`，旧头像立刻接管。

整套回滚：移除 RED-102 纸偶清单脚本与纸张皮肤样式引用，渲染器继续保留旧椭圆头像路径。不得删除 RED-68 的头像实现，直到所有角色素材完成并单独验收。
