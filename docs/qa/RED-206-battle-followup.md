# 同名手牌、观战与暗方棋组追加反馈

- 用户授权：修复同名手牌同时高亮、莉亚德琳不能弃两张同名牌、观战报错、图拉扬圣光充能文案；核实暗方选八枚棋子页的棋组模块。
- base_branch: main
- base_sha: be664e65cfb47b46019c0f045d9784c7dcd65b51
- 已 fetch，check:main-baseline 通过；分支 codex/RED-206-hand-spectator，独立目录 battle-hand-spectator-fixes。原目录出现并行更新任务后，仅迁移本次文件并撤去原目录中本次改动。
- 风险 Medium。范围：相关 data/pages、data/skills、lib/game 和必要 Colyseus 连接逻辑、tests、构建产物及本记录。
- 不包含平衡改动、存档迁移、随机算法改动、合并或发布。
- 验收：同名手牌实例独立且选择/弃置一致；大厅观战入口可收到只读战斗状态；图拉扬显示“圣光充能”；暗方选棋可保存、载入棋组。
- 验证：先复现，再定向规则/连接/页面回归、类型与静态检查、独立审查及必要真实渲染。

## 修复与复现

1. 压缩权威动作日志后，保留的 system-initialize 覆盖最新 runtimeCursors，使发牌实例游标倒退。新增真实征讨号令发牌测试，压缩前通过、压缩后三张同名牌仅两个唯一实例；修复为最新权威游标优先，旧无权威字段状态继续从日志恢复。现在真实发三张同名牌后，莉亚德琳可准确弃置其中两张，第三张保留。不改变随机算法、ID格式或存档格式；已有重复ID的进行中对局不做迁移，需新建对局验证。
2. 大厅观战跳转未携带连接配置，当大厅显式连接地址与保存地址不同，下一页会向另一服务查房。新增失败导航回归，修复为预检和跳转固定同一服务器、模式和原始房间ID。战斗页监听连接错误并显示具体原因。真实 SDK + 生产适配器的观战预检/重入/只读快照测试通过，但原始用户报错没有完整文本，不能断言其现场原因已经唯一确定。
3. 图拉扬 targeting 与执行代码的选项均从“圣光冲锋”更正为“圣光充能”；同步现有文案断言。
4. 暗方不存在独立隐藏构筑模块的逻辑，真实资源测试证实可保存、载入八枚棋子的棋组。横屏把模块移到左侧并隐藏了标题，本次恢复“棋组构筑”标题，未改变选棋或阵营规则。

## 验证

- 11文件95项回归通过：手牌/莉亚德琳、确定性运行时、手牌UI、连接恢复、大厅、真实Colyseus观战/产品房间与暗方棋组。
- 重建后追加浏览器引擎差分、房间运行时隔离、权威状态3文件27项通过；合计14文件122项。最终main基线检查通过。
- 独立审查5文件22项通过，覆盖 room-runtime-isolation、battle-authority-v2 及新增测试，无阻断问题。
- tsc --noEmit --incremental false、受影响文件 ESLint、git diff --check 通过。
- build:game-engine 通过，桌面/Android引擎和练习worker重建。首次esbuild受沙箱依赖目录权限限制，放行同一官方构建命令后通过。
- node tests/electron/battle-followup-smoke.cjs：生产选棋页面/样式与实际棋子JSON，隐藏Electron窗口验证暗方保存、载入、构筑标题可见；1280×720、844×390、390×844全部通过，截图与结果在 battle-followup/。该夹具模拟房间接口，不冒充完整线上对战。
- 首次内容文案更新导致本地profile验证指针更新的既有 PROFILE_STATE_INVALID；隔离工作区初始化后验证通过。测试夹具的两处类型错误已修正，未放宽游戏断言。

## 人工验收与交付边界

新建对局：重复获得同名圣光手牌，逐张点击确认只有目标高亮；莉亚德琳选择两张并确认只弃掉这两张。用另一Windows客户端从同一服务器大厅观战；若仍失败，保留现在展示的完整连接错误和服务器地址。暗方进入选八枚页面，横屏查看左侧“棋组构筑”，保存后新建再载入。图拉扬确认第三项显示“圣光充能”。

用户追加授权创建PR，由用户手动合并；客户端和资源包留待与其他任务统一发布。尚未制作安装包、签名资源包、合并或发布。客户端UI和引擎需重新分发，单独JSON资源包无法修复手牌或观战。正式资源更新仍需基于当前已发布资源叠加文案，不能用旧仓库快照覆盖线上平衡修改。

## PR准备复验

原基线 f73559f，创建PR前已fetch并无冲突快进合入主分支RED-207更新源功能，最新基线见上方。同步后14文件122项重新通过，全仓ESLint与根类型检查通过；基线检查通过。日志为工作区上级的 battle-followup-pr-{tests,lint,types}.log。

回退方式：撤销本PR并重建客户端引擎；未修改存档格式，无迁移。回退会重新出现旧的实例游标覆盖问题。资源文案可通过后续正式资源更新恢复。
