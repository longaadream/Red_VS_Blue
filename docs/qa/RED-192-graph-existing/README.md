# 现有流程与公共接口验收记录

对应 PR164 / RED-192。实现前基线 `e9b6918080010f30f5b2fe5f535548f80c90a257`；提交前发现新 main `4095cf289103eb3f1895d95fa74434aea30adbc0`（图标更新），同步与后续复验在本记录追加。风险 High；未合并或发布。

## 交付

- [可搜索离线流程图](flows.html)：144技能、110规则文件，6986节点，2个未展开语法块明确标记。规则manifest为108；额外两个现有规则文件也纳入清单，不宣称其已被装配。
- [逐文件覆盖记录](coverage.json)：脚本/函数/代码块数量、语法诊断。所有文件语法诊断为空。
- 编辑器直接打开已有技能/规则的流程图；能沿关联入口跳转，修改代码节点并保存。没有脚本的被动借助棋子模板规则索引，不能只画一个虚假的结束节点。
- [接口与限制](../../technical/CONTENT_FLOW_RUNTIME.md)：可信 `flow` 公共能力、跨回合 extension、源码图与纯参数图的边界。
- 鸣人分身动态显示本体可见状态，真实状态与规则不复制。

## 已运行验证

| 检查 | 结果 |
| --- | --- |
| 首轮流程、运行时、分身显示、原类型图 | 4文件27项通过 |
| 最后鸣人完整回合与 flow 接口 | 2文件28项通过；含无分身/2分身、最后一点进度、AP刷新后奖励及不重复领取 |
| game + electron 扩大验证 | 133文件1457项，1444通过、13失败；见下方分类，未改冻结快照 |
| 房间隔离复查 | 8项通过；扩大时2项为资源包 active.json 同时写入 EPERM |
| AI规划复查 | 整文件15/16通过，仍有5秒超时；隔离该场景1项通过（4.27秒），保留性能阈值风险 |
| 新代码定向 ESLint，空抑制表 | 通过；未修改仓库抑制配置 |
| 编辑器 TypeScript | 通过 |
| 根 TypeScript | 源码无新增错误；候选产物目录被根include扫描，2个缺失导入错误，不能宣称规范根命令全绿 |
| 编码、静态词法兼容检查、diff whitespace | 通过 |
| Windows候选打包与文件完整性 | dist/editor-flow 目录/portable/NSIS；包验证通过 |
| 实际打包EXE冒烟 | 现有鸣人技能图、跳转回合开始规则、节点编辑保存保留其他字段、原类型图连线/环路拒绝/保存重开/外部冲突保护均通过 |

扩大验证其余10项为先前已记录问题：AI语义1、静态旧数量2、动能措辞1、targeting冻结hash1、教程VM夹具缺全局3、嵌入PostgreSQL运行库2。`skills.ts` 单文件lint仍报告158个既有any；HEAD原文件同样有这些诊断，未批量放行。单独字段 `flow` 只加入可信运行时词法审计，不放宽隔离VM或外部资源包准入。

机器输出在忽略目录 `logs/red192-graph/flow-*`。截图：[技能](../RED-192-graph/existing-skill-flow.png)、[规则](../RED-192-graph/existing-rule-flow.png)、[交互摘要](../RED-192-graph/smoke.json)。旧 portable 被占用，已停止本任务旧构建，使用独立 `dist/editor-flow` 完成验证，没有关闭用户应用。

## 独立审查与人工检查

独立审查提出并已修复：模板被动缺少真实规则入口、玩家规则误读事件玩家、兼容triggerSkill丢事件上下文、规则入口意外获得主动强制离场能力。补了对应查询、事件写回与入口约束回归。

建议打开离线清单搜索“仙人模式”，核对施放与 `rule-naruto-sage-tick` 的不同入口；在编辑器临时项目打开同一技能，跳转规则后点击节点、保存重开。实战查看本体获得/移除可见状态时分身外观同步，但分身不获得圣盾等实际防护。鸣人准备进度与状态持续时间是不同字段：状态不自动到期，准备仅己方beginTurn推进。

回退：回退本次增量提交，并同步回退已经使用新 `flow` 接口的作者内容；备份项目，避免旧运行时加载新接口脚本。现有存档版本机制与外部包准入不变，不降级进行中的对局。
