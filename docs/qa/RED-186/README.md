# RED-186 页面美工升级验收记录

## 基线与范围

- 2026-09-07 复核 `origin/main`：`e7d47cd1b3598237e04752cc77306264babf1deb`，ahead 0 / behind 0。
- 在独立分支 `codex/RED-186-art-upgrade` 实施，保留原工作区的其他任务改动。
- 主菜单、大厅、房间、PVE、训练入口、选角/预设卡组、角色图鉴、地图图鉴、资源包、开发工具、回放共 11 个 HTML 接入共享样式。
- 暖暗木桌、低饱和纸张、手绘轮廓、漫画标题及本地图标保持与已认可的对战样本一致。正文使用系统字体保证长文本可读。
- 第一阶段仅新增样式引用和页面作用域属性。追加阶段将 `battle.html`、`pieces.html`、`piece-selection.html` 的卡面抽为共享呈现，正式对战页现在也引用 `hand-card-face.js`；其他 9 页原有内容/脚本仍保持不变。预览服务器继续提供已有对战美术适配层。

## 验证

- `npm run check:main-baseline`：通过。
- `node docs/qa/RED-186/check-art.cjs`：追加阶段的 9 个非卡面改动页面剥离样式接入后与基线逐字一致；三份 CSS 解析通过；11 处资源引用均可本地解析；字体许可存在。结果见 `static-verification.json`。三个共享卡面页面通过下面的运行时测试验证。
- `npx vitest run tests/ui/main-menu-layout.test.ts tests/pve/prototype-page.test.ts tests/electron/piece-selection-resource-fallback.test.ts tests/game/map-selection.test.ts`：4 个文件、40 项测试通过。
- `git diff --check`：通过。
- 浏览器检查主菜单 1366×768、1280×720、844×390 横屏；无页面横向溢出，小横屏主面板底部 334px、底栏顶部 344px，保留操作间距。
- 实际操作主菜单进入图鉴、训练配置并开始训练；训练棋盘和回合界面加载成功。检查账户弹窗开合、角色技能展开、地图网格、选角计数与阵营切换、资源包 URL 输入区展开。
- 检查开发工具、回放、PVE、大厅、房间的现有空态/错误态布局。
- 独立审查发现预设卡组选中态被公共背景覆盖、回放详情文字对比不足，已加入更明确的选中态和纸面文字颜色。

## 验证边界

- 本地静态预览没有联机/PVE 后端，PVE 接口返回 404；大厅和房间以未连接/缺少参数状态检查，未验收真实创建房间、准备和联机对战。
- 未提供有效 Trace 文件，回放和开发工具的真实数据导入/播放未验收。未进行 Electron 打包验收。
- 手机竖屏不是当前游戏的正式游玩目标；此次重点验证桌面与小尺寸横屏。
- 原始 `npm ci` 遇到已有 Colyseus/Zod peer 依赖冲突；本工作区使用 `npm ci --ignore-scripts --no-audit --no-fund --legacy-peer-deps` 安装测试依赖，未修改依赖清单或锁文件。
- 改动尚未提交、合并或发布。

## 预览

主界面：`http://127.0.0.1:4180/index.html`。当前使用仓库外层已有 `samples/parchment-battle/server.cjs`，设置 `RVB_SKIN_REPO` 指向本工作区、`RVB_SKIN_PORT=4180`、`RVB_SKIN_HOME=/index.html` 后运行。预览服务器对非战斗页面直接提供本工作区资源，对战页面叠加之前的样本适配层。

生产页面样式均位于 `data/pages/css/tabletop/`，资源位于 `data/pages/images/tabletop/`，不依赖预览服务器的注入即可生效。

## 追加：真实相关手牌与美漫头像

- 图鉴、选角详情和对战角色详情统一引用真实手牌的 `HandCardFace` 卡面。卡面仍展示实例动态费用、强化说明及徽标；详情中的卡牌只读，不引入出牌处理器。静态卡面自动增高、完整换行，图片失效时显示名称首字。
- 追加测试：`npx vitest run tests/ui/hand-card-face.test.ts tests/game/battle-card-metadata-runtime.test.ts tests/electron/piece-selection-resource-fallback.test.ts`：3 文件、25 项通过。覆盖动态展示、只读预览、转义、本地图片路径以及权威手牌选择流程。
- 独立审查未发现阻断问题；已修复小屏图片高度的选择器优先级。
- 35 个角色头像由内置 ImageGen 重绘，打包为 512×512，保持原文件名。32 个 JPG 位于 `public/`，3 个 PNG 位于 `data/pages/images/`。生成来源与修正记录见 `portraits.json`；风格提示词见 `portrait-art.md`。
- 用户指出的佐助、拉法姆、带土、鼬、飞段多余的角，以及蓝染眼下十字，均已用 ImageGen 修正并替换为修正版。
- **未完成：毒液头像。** 图像工具多次返回 `moderation_blocked`（类别 other，未提供具体理由），保留原头像，不计入已重绘数量。
- 浏览器确认图鉴 36/36 头像均加载，其中 35 张为 512px 新资源；拉法姆的相关诅咒卡插画、费用和完整效果可读；选角莉亚德琳三张圣光卡插画均加载。844×390 横屏详情无横向溢出，说明没有行数截断，弹窗内可滚动。
- 头像替换不改变 `data/pieces` 或资源包解析逻辑。历史记录、棋盘及详情沿用同名资源；自定义资源包继续拥有自己的图片来源。
- 独立只读资源复核通过：35 项清单均匹配棋子图片文件名，文件存在且为 512×512；毒液原文件保持不变。

## 追加：美漫战斗特效与历史标记

- 再次 fetch 并通过联网 main 基线检查，HEAD 与 origin/main 仍同为 `e7d47cd1b3598237e04752cc77306264babf1deb`。
- 正式 `battle-renderer-3d.js` 与战斗 CSS 接入描边漫画箭头、青色来源八角环/金色目标菱形环、范围排线，以及上移的伤害/治疗数字爆点。新几何与材质均随原表现对象释放。
- `battle-action-vignette.js` 从已投影的可见结果显示至多一个漫画贴字；缺少目标位置时不显示，不猜测暴击或隐藏结果。使用既有投影和队列，不修改战斗状态、时序或命令。
- 实机检查发现并修复空坐标被转为 `(0,0)` 的旧问题，以及按需渲染下历史高亮/静态特效没有立即刷新或清除的问题。独立审查复核通过。
- `npx vitest run tests/ui/battle-renderer-3d-runtime.test.ts tests/ui/battle-action-vignette.test.ts tests/ui/battle-action-history.test.ts tests/ui/battle-motion-feedback.test.ts`：4 文件、58 项通过，覆盖原生命周期、几何、倍速/略过，以及新增正常字效、保密结果、缺失死亡目标、按需绘制/减少动态回归。旧宽度和颜色断言按本轮明确美术变化调整，未修改快照。
- 浏览器在独立训练预览页实际释放圣光惩击、圣光治疗并执行移动；捕捉到治疗“恢复!”和伤害“砰!”结果帧。1280×720 与 844×390 的字效均位于目标附近，未遮挡底部手牌操作；历史点选显示来源/目标与箭头。控制台无错误。原用户对局未刷新。
- JavaScript 语法与 CSS 解析/本地资源检查通过。动作跳过、倍速、减少动态与清理的完整时序由自动测试验证，浏览器未重做所有联机流程。
- 这轮是现有基础演出的风格升级；角色专属变身动画未新增。联机后端、Electron 打包仍沿用上述验证边界。未提交、合并或发布。

## 追加：行动记录聚合、历史局部棋盘与玩家效果（2026-09-07）

- fetch 最新 main 并 fast-forward 到 `afbbda5f1d4f7179ec2b2d3a81fceaee9fb157a8`（RED-189），保留此前美术修改。验收末次联网 `npm run check:main-baseline` 通过，ahead/behind 均为 0；一次 GitHub 临时连接失败后重试成功。
- 行动记录将同一动作内连续、同源、同类型的多棋子伤害/治疗、状态和多地格效果合并；原始子事件完整保留，不跨来源或不同玩家目标合并。子行头像代替重复全名，名称仍有悬停说明，部署与明确归属玩家的效果显示玩家主语。
- 每个事件附带只读的行动开始位置与相关地格上下文，历史面板独立显示这些内容，不再覆盖当前棋盘。范围只包含该事件已披露的相关棋子及附近地形，不是整盘 Trace 回放。旧记录无历史上下文时明确提示，不能用当前棋盘补造。
- 强制位移从被移动棋子的权威 from/to 字段显示起点、终点及箭头。历史上下文跟随查看者保密投影，私有子事件的目标与地形不会进入公开根事件。
- 玩家资源牌新增可折叠“全队效果”，显示可见状态与玩家规则，艾露恩守护显示状态并在消耗后给出已消耗提示。万花筒、灵压、动能的短文案精简；关键词完整细则默认收起，可展开阅读。
- 浏览器实际释放天照：9 个地格合成 1 条，连同行动点变化共 2 个结果；悬停/点击可在独立历史板看到全部 9 格及坐标。实际移动阿尔萨斯、释放千鸟，核对历史起点 (12,6) 与终点 (12,11)，当前棋子位置与历史板独立。
- 检查 1280×720、844×390，修复小屏卡背遮住预览关闭按钮、旧 transform 提示产生父堆叠层、phase-bar 裁剪玩家效果的问题。预览有独立滚动和关闭入口，进入目标选择/阻挡式弹窗自动清除。关键词细则展开、玩家全队效果展开/收起均实测；控制台无错误。原用户对局未刷新。
- 受影响测试：15 个文件 177 项通过，覆盖行动历史/事件投影与隐私、玩家效果、RED-189 角色、关键词、渲染与动作反馈、卡面和菜单。末次交互调整后重跑历史与事件两个文件 36 项通过，增加多目标不同伤害量聚合和目标选择清除预览断言。
- 完整 `npx tsc --noEmit --pretty false` 通过；`npm run build:game-engine` 成功生成页面实际使用的浏览器引擎。JavaScript 语法、5 份 CSS 解析、12 处本地资源和 `git diff --check` 通过。
- Medium 独立只读审查及最终布局修复复核通过；已修复审查发现的历史头像类型字段和跨玩家状态聚合问题。
- 继续沿用静态预览/无联机后端的验收边界；未提交、合并或发布。

## 纠正：大棋盘历史还原、简洁关键词、指定玩家图标（2026-09-07）

本节替代上节“小棋盘”和“全队规则折叠列表”的交互结论，按用户最新纠正实施。

- 历史查看临时让**当前大棋盘**回到行动开始时的完整展示局面，再绘制目标环、各自起终点箭头和伤害/治疗字效。保留镜头，显示“查看历史/返回当前”，Esc 同样返回；返回时恢复最新接收的战局。真实状态未回滚，历史期间阻断行动输入。
- 表现层缓存最多 20 个根动作的查看者可见棋盘副本，不缓存手牌或规则代码。新查看者、首帧、追帧中无可靠前态的记录不伪造历史。旧稀疏事件数据只用于坐标说明。缓存不是持久化回放。
- 玩家栏采用显式图标白名单：艾露恩守护（月盾）与古尔丹裂魂（灵魂碎片），各自有简洁紧邻提示；守护消耗后图标消失，天照后台触发规则与幸运币开局规则不显示。两张 SVG 原创绘制，目录 images/effect-icons/player-*.svg。
- 直接缩短动能、万花筒和灵压正文；新增天照关键词并挂到佐助三项相关技能，天照之炎只保留技能自身的目标/范围描述。移除“规则细则”折叠区，未改变技能执行代码和数值。
- 浏览器真实操作：阿尔萨斯移动后回看，棋子回原位、目标箭头出现；点击返回后恢复实际新位置、手牌透明度恢复为1，继续选佐助并释放天照成功。历史期间点击结束回合未提交行动。九格效果在大棋盘标记，844×390 返回入口可用。新建真实训练局选择古尔丹和泰兰德，两个玩家图标均出现，桌面及小横屏提示可读，无侧栏。天照关键词在三个技能入口可打开。控制台未见错误。
- 发现并修复：切换查看者当帧误用旧模型捕获；旧地格 InstancedMesh 缓冲未释放；返回时镜头 resize 重入激活历史。新增查看者单根隔离、整盘位置/镜头恢复、最新状态恢复、输入拦截和同步 resize 回归。
- 10 个相关测试文件 125 项通过；完整 TypeScript 检查和浏览器引擎构建通过，JS/CSS/本地资源及 diff 空白检查通过。独立 Medium 最终复核无遗留阻断。
- 仍未进行联机服务环境和 Electron 打包验收。历史仅覆盖本次页面会话留存的已接收画面。未提交、合并或发布。

- 基线说明：本轮开工 fetch 成功，origin/main 为 afbbda5f1d4f7179ec2b2d3a81fceaee9fb157a8；交付前 check:main-baseline 两次遇 GitHub 443 连接失败（FETCH_FAILED/REMOTE_UNREACHABLE），未宣称最终远端复核通过。用户明确授权同步本轮实现与测试结果后，Linear RED-186 已追加成功。

### 2026-09-07 数字反馈、赐福与训练历史修复
- 基线：本轮开始 fetch 成功，origin/main = afbbda5f1d4f7179ec2b2d3a81fceaee9fb157a8。交付前 check:main-baseline 因 GitHub RPC 连接重置返回 REMOTE_UNREACHABLE，未声称该次远端检查通过。
- 伤害/治疗去掉重复拟声标签，真实 −X/+X 使用漫画爆点底形，位置改为投影点侧上方（+48/-44），图层提升到12。已确认死亡移入 graveyard 的可见棋子仍显示损失血量；普通移除不推断死亡。
- 月神赐福：仅投影 elune-blessing-buff 的有效 uses，HUD 新增月亮圣光卡原创 SVG；消耗后自动隐藏，不展示其他内部 buff。
- 训练添加棋子成功后结束放置模式，解除行动记录强制收起；失败保留模式可重试。旧记录保留独立棋盘快照，新动作包含新增棋子。
- 修复历史返回按钮继承 pointer-events:none 导致无法点击，预览工具条显式开启指针交互。
- 自动：6 个相关测试文件 85 项通过；全项目 tsc 通过；check-art.cjs 通过。独立审查提出致死遗漏，修复后复核通过。
- 浏览器 1280×720 实测：圣光惩击 −5；放古尔丹后旧记录6子/安度因12HP，返回当前7子/7HP；圣光治疗实际+3；训练放置泰兰德并进入蓝方回合，月神赐福图标加载成功，圣光惩击 −10 后图标消失；控制台 error 为空。

### 2026-09-07 跨回合/对方历史棋子消失回归
沿用既有大棋盘历史修复合同（data/pages、tests、docs，Medium，保持规则/网络/存档不变）。本轮 fetch 与交付前 check:main-baseline 均成功，base_branch main，base_sha afbbda5f1d4f7179ec2b2d3a81fceaee9fb157a8。
复现：训练红方圣光惩击→结束回合自动换蓝方→悬停旧出牌记录。控制台 RangeError: Maximum call stack size exceeded；缺失快照回退触发 showHistoricalBoard→_buildTiles→resize→highlightOverlay→重复回退，清空棋子后中断。
修复：live返回幂等；历史缓存按查看者隔离保留，每人20根动作；训练在动作前分别保存双方投影，不切换当前玩家来取快照；普通动画入口保留快照viewer ID。缺失快照不伪造历史、不重建当前棋盘。新增3项回归覆盖缺失快照重入、跨viewer隔离、普通动画链快照归属。
验证：4文件62项通过，tsc通过，main检查通过；浏览器跨回合看红方旧动作显示6子/安度因12HP，返回当前6子/7HP。审查指出动画入口丢viewer问题，已修复并加回归。

## PR 候选版本：正式美术接入
先前章节记录的是迭代过程；本次候选已将原预览服务提供的美术正式纳入源码，**合并后不依赖外层 samples 或运行时替换**。
- battle.html 直接加载 tabletop-battle 的五份样式和两个表现适配器；木桌/纸纹/字体/卡背/图标均本地提供。renderer 的材质、光照、高亮和棋子尺寸使用已验收参数。
- character-dock 保留原生技能可用性与意图入口、敌方无释放；refinements 仅读公开手牌数量，不读取对方手牌。
- 固定样本仅保存在 docs/qa/RED-186/showcase.js。运行 node docs/qa/RED-186/serve.cjs 后，默认4181端口；只有 mode=training&sample=1 会注入样本。正常页面不会改写开局。
- 正式页浏览器验收：1280×720 敌方无释放、角色/历史共存、真实出牌−5、无 /skin 资源依赖，控制台无错误。
- 19文件191项相关测试通过；包括新增正式资源打包、隐藏状态和普通对战历史链测试。浏览器引擎构建通过；独立迁移审查5文件42项通过。
- 广泛测试发现main已有旧技能名断言及7个状态缺分类：按现有中文回退更新断言、补齐显式状态分类和漫画图标，临时侧射账本保持隐藏。
- 限制：35个重绘头像，毒液仍原图；未验证联机服务、Electron/Android整包。无伤害、经济、随机或存档规则变更。
- 回退：撤销该PR提交，恢复原入口、资源与生成引擎，不涉及存档迁移。
