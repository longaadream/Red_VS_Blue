# 基础战斗音效验收记录

本轮承接用户“下一项”：完成伤害、治疗、移动、传送反馈及本地试听。范围为表现层音效、主菜单音量、试听页与相关测试/文档；不改规则、网络、存档或正式发布渠道。风险 Medium，需独立审查。base_branch: main；base_sha: 59aff06dcc5d296cd3a9f1f1ec8c53d773589d73（2026-09-16 fresh fetch）。

验收：实际结果触发；范围同类效果去重；失败及零结果静音；历史/跳过不补响；首次输入解锁；静音和后台立即停止；音量可恢复；最多12声部。音色舒适度由用户试听确认。

本轮补修：原先音量只在下一次 play 时读取，已排入 Web Audio 的短音效不受静音影响；现在音量变更同步播放器增益，并终止剩余声部。隐藏页面同样终止声部。原先静音时 pointerdown 不创建音频上下文，拖动滑杆恢复后首次 change 可能无声；现在 input 保存非零音量后主动解锁。存储不可用时仍保留本页设置。

自动验证：`npx.cmd vitest run tests/ui/battle-audio.test.ts tests/game/skill-presentation-ui.test.ts --maxWorkers=1`，15项通过。静音/恢复两项在修复前失败、修复后通过。独立审查发现重连首快照补响，已复用演出队列reset重建恢复基线；恢复期间及首快照前静音。新增4项真实队列回归覆盖这两段时机、两种首快照路径、后续新动作正常发声、同批AOE一声/不同批多段各响。VM内撤销修复后恢复用例确认失败；修复后独立复审未发现剩余确定问题。

浏览器：localhost:4181 的现有 QA 训练场景中，通过正常点按释放圣光治疗，阿尔萨斯9→12 HP、AP10→9；释放圣光充能，显示强化状态、AP9→8。此处核验真实操作和表现链；不将工具点击视为人工听感验收。未启用安卓模拟器。

试听页按钮及45%→静音→45%控制正常，试听页与战斗页无控制台警告/错误。定向ESLint、diff空白检查和main-baseline通过。浏览器实测不替代Android真机音频解锁和扬声器听感验收。

试听：运行 `node scripts/build-official-site.mjs` 和 `node scripts/preview-official-site.mjs` 后打开 `http://127.0.0.1:4188/sound-preview.html`。四个独立按钮复用游戏代码，可调音量与静音。

回退：移除本次 BattleAudio 接线/模块与音量控件，保留原提示音回退路径；不涉及持久化对局数据。当前为源码及本地预览，尚未打包安装程序或上线。

## 2026-09-16 数值分级与震动

沿用上述 main 基线。伤害/治疗按实际结果 1–3、4–7、8+ 三档；移动按权威结果端点视觉距离 1–2、3–4、5+ 三档，不重算规则消耗。同批同类取最大值，不累计范围目标音量。传送保持独立音色。重伤害触发最多 3px / 180ms 棋盘震动，主菜单及试听页可关闭；尊重系统减少动态效果，用户点按立即停止。历史、跳过、恢复基线不补播。

三份测试 battle-audio、battle-impact、skill-presentation-ui 共 22 项通过，独立复审未发现确定回归；复审指出无生产者的 path 分支，已移除，仅消费现有端点。试听页现在为伤害/治疗/移动各三档加传送，共十个按钮，复用游戏模块；浏览器确认重伤害按钮及数值反馈。听感仍需人工试听，未启动模拟器，未发布安装包。

## 按钮反馈

用户追加按钮音效：Low 风险，沿用同一 main 基线，范围为共享音效、button-audio 委托监听、玩家页面接入和试听页。短促点击声沿用音量/静音；真实 click（含触摸与键盘激活）触发，禁用/inert/显式静音按钮及战斗音效试听按钮跳过。pagehide 清理，pageshow 恢复。按钮不改业务事件或规则。两个定向测试文件共 9 项通过；未打包发布。

对局设置：battle.html 工具栏新增齿轮，原生模态对话框支持音量/静音和震动开关，复用已测试的控件与持久化。返回/Escape 关闭并恢复入口焦点；菜单内按键不冒泡至对局快捷键。菜单不暂停联机回合计时。Low 风险，不修改规则。

工具栏遮挡修复：原桌面避让仅覆盖宽>=1100且高>=601，手机靠右对齐溢出可能挤出首个离开按钮。统一横屏工具栏宽度变量和玩家区域留白，工具栏从左开始并可横向滚动，玩家卡不压缩；修复手机设置按钮误用返回箭头。使用实际 battle.html 样式/工具栏的隔离布局夹具，启用联机状态并放入8张长名玩家卡，在1266×793、1000×650、844×390浏览器检查：离开按钮在工具栏内，中心命中自身，玩家栏与工具栏不相交。此为布局验证，未发起真实排位。
