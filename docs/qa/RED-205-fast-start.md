# RED-205 启动优化合同与验证

用户授权：根据本机计时优化启动，主菜单提前显示、后台准备本机服务。
base_branch: main
base_sha: 23eb55067fb221d8b4ff3cd457d5465446b57bd7（2026-09-13 fetch，基线检查通过）

风险 Medium。范围：electron-client/、data/pages/index.html、tests/electron/、docs/qa/，本地计时与候选产物。保留资源恢复完成后再读取菜单的顺序，避免菜单读取恢复前的资源；数据库和对局服务移到菜单之后。入口等待同一次启动；准备中提示、失败重试、关闭取消均需可验证。不修改规则、存档、数据库选型、完整性验证，不发布或覆盖用户安装。

验收：延迟数据库时菜单已显示；资源恢复中仍保留启动页；提前点击入口只等待同一次启动；失败可重试；关闭后不启动迟到服务；官方更新不得与初次后台启动重叠；Windows 同机分别计时菜单和服务就绪。测试包括实际启动函数回归、入口并发/失败/关闭、Electron 类型检查、lint 与本地候选。回退：撤回独立优化提交。

本合同记录 RED-205 的后续启动性能优化范围。

## 实现

- 资源恢复检查完成后加载主菜单，菜单加载完成再后台启动数据库及对局服务。保留资源恢复边界。
- 开局与主机信息查询等待同一个初次启动 Promise；初次启动期间禁止应用官方更新。
- 桌面内置状态脚本在每次主菜单加载时运行，兼容旧资源包，准备中可浏览菜单，失败可重试。服务就绪只更新本地地址，保留玩家已选择的 LAN/远程服务器。
- `get-mode` 需对局资源身份校验完成才报告 ready。背景启动中退出也保留安全退出页，并取消迟到的开局。
- PostgreSQL 文件哈希读取改为异步；1618 项完整性验证仍全部执行，校验失败仍拒绝启动。哈希计算及目录元数据检查仍同步。
- 最终无需修改游戏内容页面。修改限于 Electron、相关测试及本文件。

## 验证结果

修改前新增“后台启动未完成时菜单已显示”的回归失败；修改后 7 个测试文件共 74 项通过，含真实 PostgreSQL 初始化一次、持久化、崩溃检测、无效运行库拒绝、异步校验并发等待、启动取消/重试及更新保护。Electron 类型检查、修改文件 ESLint 与 git diff --check 通过。独立审查指出的 LAN 地址覆盖、返回菜单丢失状态、背景启动关闭拦截均已修复并复核，无未解决的 P1/P2。

本地候选以现有 0.1.1 Windows 解包产物为基础替换编译后的 Electron 文件。原包来源 2a785e03e4613cef61fc0f90263279d10ea2863d，其 app/lib/data/public/electron-client 源与本任务 main 基线无差异。保留原运行库和游戏资源，未发布、安装或覆盖用户数据。

同机本次比较（自动更新关闭、各自独立数据目录、操作系统文件缓存未清空）：

| 场景 | 原版菜单完成 | 优化菜单完成 | 优化可开局 |
|---|---:|---:|---:|
| 新数据目录首启（运行库已有缓存） | 11.814 s | 2.709 s | 12.009 s |
| 同目录第二次启动 | 4.927 s | 2.655 s | 4.899 s |

主菜单以 document.readyState=complete 计时；可开局需 ready=true、对局 Profile Identity 有效、恢复状态 ready。原版可开局分别为 11.887 s 和 4.989 s。此改动主要减少菜单等待，并未声称把首次建库本身缩短到 2.7 秒。新复制候选首轮曾出现大幅文件读取波动，不作为同缓存条件的性能结论，也不承诺所有电脑或磁盘冷启动均达到上述时间。

实包额外验证：

- 首次准备中同时调用两次 ensureLocalAuthority，均返回 ok=true；只启动一次对局服务。
- 准备中进入 tutorial.html 再返回 index.html，准备提示仍可见，就绪后本地地址正确。
- 新数据目录启动中在 2.763 s 关闭，保留安全退出页，完成正在进行的建库后停止数据库，进程以 0 退出；未启动迟到的对局服务。整个进程在 12.596 s 结束，因此首次初始化中的安全关闭仍需等待几秒。
- 最终候选全部测试进程正常退出。页面截图确认菜单布局及更新入口正常。

证据位于工作区外层 startup-measurement/comparison-final（原版）、comparison-verified（最终候选）及 final-tests.log。首次测试工具的菜单探测错误和早期仅用 ready 标志的采样均已排除，最终采样包含身份校验。

## 人工试用

运行本工作区 dist/启动优化预览.cmd。启动器使用独立 preview-user-data，保留原安装与存档。观察菜单先显示、准备提示消失后可开局，并可测试准备中进入教程、点击训练或关闭窗口。

用户已试用本地候选并确认验收通过，要求创建 PR 后自行合并。本次不发布版本。
