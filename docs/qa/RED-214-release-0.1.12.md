# 0.1.12 发布候选

base_branch: main
base_sha: bc76ce78e9014f50e5ef1ea445895de8979b0ea1
Linear: RED-214。用户明确要求推出 0.1.12；风险 High，复用现有发布流程。

本版本整合 RED-212 毒液修复、RED-213 角色平衡及 v0.1.11 发布准备提交。
共生拖行命中相邻敌人时不移动但施加定身1；利爪撕裂被保护时仍合法结算。
阿方费用/冷却、麦克斯速射两次和全队迅捷、死神死亡绽放费用、索尼克追踪攻击伤害按已确认规则更新。
普里莫防御1、愤怒铁拳CD1，保留原被动并新增流星腰带：AP1/CD3，自身3×3友军代伤2回合；使用普里莫防御/护盾，仅己方回合结束扣持续且施加当回合不扣。

Windows、Android及服务器必须同步新引擎；仅资源包不能为旧引擎增加代伤接口。
Android versionCode 31，保留原公开签名。两端产物来自同一干净提交。
资源继承线上1.0.9与RED-212/213累计候选，发布前执行历史校验及实际规则回归。

验证记录将保存在本地 dist/RED-214；候选未完成验证前不作为正式可用版本。
回退保留旧服务器目录、状态备份与v0.1.11资产；Android不强制降级，公开后问题用更高版本修复。
人工验证：更新后新开服务器对局，测试毒液两技能、流星腰带两轮持续和护盾、速射两次上限；手机确认系统安装后账号/存档保留。

历史资源核对：461项JSON与累计候选语义相同，14项仅格式差异；保留Git独有的3项页面配置。
候选21张adventure图片完整继承，其中arthas.jpg/reaper.jpg不在Git历史，保留在配套外部资源包中；其余19张与Git页面图片逐字节一致。
历史校验记录两项显式更新：规则manifest新增流星腰带且不删除原规则；df3a91c74已有小乌再生描述简化，代码/回复1行为未改变，保留该短描述。不是恢复旧资源覆盖新修复。

发布前检查：测试夹具类型错误已修复（未改生产类型），全项目typecheck通过；五文件48项测试和定向ESLint通过。更新链39项、历史行为13项通过；资源包签名/解析/种子213冒烟及隔离安装激活重开通过。首次长路径隔离安装触发Windows EPERM，改用短临时目录后完成验证；未操作用户活动资源库。
