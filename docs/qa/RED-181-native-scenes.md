# RED-181 原生场景样例验证

日期：2026-09-09。初始基线 main `012e595fe2affb29725c45df0c2cd819704768a0`。展示前主线合入 RED-197 文档更新，分支已快进到 `0debcd34126803686719b9ce87a490fe4795d13d`，原生战斗文件没有变化，5项定向检查重新通过。同步后的远程基线复查曾遇到连接重置。风险 Medium，仅本地样例初始化适配。

## 结果

- `node output/pve-roguelike/native-scenes/check.cjs`：5 项通过。覆盖三地图原生地形属性、320格、可行走且互不重叠的5个出生位置、双方连通性、1/0初始AP及上限、非法配置拒绝、仅装饰POST初始化的边界。
- `scenes.js`、`scene-runtime.js`、`serve.mjs` 的 Node 语法检查通过。
- HTTP 精确比较：8876 场景响应去掉两条注入标签后，与当前 `data/pages/battle.html` 完全一致；8876 与原生8875的 `battle-renderer-3d.js` 响应完全一致。
- `git diff` 确认正式 battle.html、3D 渲染器、tabletop 皮肤没有修改。
- 浏览器实际打开原训练场作参考，随后加载遗迹入口、断桥密室、泉边营地。三场景均进入原生行动阶段、显示现有动漫棋子及原生木桌/倾斜棋盘/方格/控件，没有旧俯视样例元素。原生场景切换控件正常工作。
- 遗迹入口：选中猎空后点击高亮格，由原引擎执行移动，AP从1降至0，动作历史出现移动。
- 修正后重新开局，依次结束两方回合：当前行动方AP依次为1、1、2。随后只选中一次猎空，连续点击两个高亮格，AP从2→1→0，动作历史出现连续两次移动，中间未重新选棋子。

## 独立审查

`review_native_scenes` 只读审查发现初版只恢复AP而未恢复训练模式10点上限，已同时恢复双方AP及上限为1/0。审查者复查并独立运行5项检查通过，无遗留阻断项。浏览器另验证换回合正常增长。

## 实际边界

这是场景布局与原生交互候选，不是完整PVE。没有接入迷雾、巨型地图探索、撤离、构筑、营地结算、存档、联机或自动AI。现阶段切场景重新开局、敌方使用训练手动控制。样例初始化发生于旧训练初始化之后，不能将其初始化Trace当作正式PVE验真回放。正式实施必须前移地图初始化。

没有运行全仓测试、安装包构建或手机设备实测。被用户否定的独立doodle样例已删除实现，只保留停用说明，不将其中未完成测试描述为通过。

## 试玩与回退

保留的浏览器入口为 `http://127.0.0.1:8876/`，左下选择三个场景。按PVP原有方式点击己方棋子与高亮格，或拖动棋子/镜头并缩放；场景切换会重新开局。停止8876样例服务器即可回退，不影响正式PVP源码或游戏存档。

关联：RED-181。未创建PR、未合并、未发布；等待用户评价场景方向。
