# RED-181 试玩入口改进

base_branch: main
base_sha: ebe47eee059d18b5282856759a1a04edbc553d67

用户授权边讨论边修复已明确的问题。普通更新发现后必须完成；检查失败允许兼容本地版本离线进入。其余设计逐项确认。

本轮范围：electron-client 启动与文字选择、lib/server/colyseus 的 PVE 按需加载、data/pages 棋盘悬停及合法目标表现、教程运行时/样式/角色技能提示、对应 tests 和本文。用户追加批准教学实体示范动画、部署落点、技能顺序配合提示。保留已有资源包与存档，不发布版本，不更改签名。中风险；更新流程设计确认后单独实现并验证。回退以撤销本轮提交恢复，不迁移用户存档。

验收：缺少 PVE 的旧资源包不阻止 PVP 服务启动；创建 PVE 房间给出明确资源提示；完整资源包 PVE 仍可创建。普通文字不可选，输入框可编辑和复制。格子悬停随棋盘变换，合法目标以权威候选集合高亮。

验证：对应回归、类型及静态检查，随后进行客户端交互检查。完成前不以旧测试包的验收结果代替本轮结果。

## 已确认的交互方案

本轮启动检查实现合同（High，用户此前已授权强制更新）：Electron主页先弹检查窗，主进程保留首次检查门禁，已发现更新即使下载失败仍阻止进入；未发现更新的检查错误可在本地服务就绪后离线进入。复用签名验证/原子启用/安装确认，不修改发布源或签名；回滚撤销门禁与页面改动即可，不改变存档。范围electron-client、official-updates.js及对应测试。验证首次检查、失败重试、已发现更新失败不可跳过。安卓后续单独接入现有维护页。

本轮明确替换角落教学：顶部任务条独立显示目标，操作附近对话框说明当前规则，部署棋盘逐格标号1–6（合法第6格来自权威集合）。沿用完成操作自动推进，说明仅按需展开。中风险、表现层，测试教程状态流及Three.js提示生命周期，桌面/横屏截图验收。

本轮实现与验收：任务条置顶、对话框锚定部署/技能/资源控件，替换默认角落卡；棋盘数字尺使用独立可释放纹理，选择无遮挡示范路径。57项教程与渲染器测试通过，ESLint通过，独立审查无阻塞项。1280×720及844×390网页截图验收，修复旧横屏列布局让任务条换行的问题；未打包客户端。

用户纠正教学形态：取消默认长文剧本卡，改为一句操作条；部署以权威合法格集合展示可放/不可放地格，行动点旁显示增长示意，技能按钮就地引导。说明和回看按需展开。允许修改教程 CSS/JS、battle.html、Three.js 提示层及对应测试；仅表现层，中风险。验收桌面和低横屏无需滚动即可看到当前指令，部署标记跟随棋盘。

已实现默认折叠短指令、棋盘原生红叉、5/6格标尺和资源旁增长示意。桌面/844×390横屏检查后去掉部署重复状态提示，避免遮挡标尺；复盘保留“继续”主按钮。57项教程/渲染器回归通过，独立审查指出的折叠隐藏继续按钮已修复。增长示意为教学动画，尚非真实回合资源变化的逐格补满动画。

追加修复：选中棋子后的移动模式在恢复菜单/局部刷新后重新从权威合法格查询恢复；不放宽行动锁或规则。教程补充部署安全距离、行动点增长及赐福按钮指引。范围为 battle.html、教程 JS 和对应测试，中风险；验收覆盖已选中直接移动、目标选择期间不能移动以及教学指引内容。

追加验证：已选中但移动标志清空时可直接发起合法移动，结算锁下不能发起；拾取结晶后指向乌瑟尔赐福按钮。三份回归共 26 项通过，独立审查复跑通过、未发现阻塞项。部署规则为与所有存活场上棋子的曼哈顿距离 >5，行动点每个自己的新回合上限 +1 至10并补满。尚需玩家在实际客户端复验偶发移动症状。

- 启动先检查客户端与资源更新；发现更新必须完成，检查失败允许兼容的本地版本离线进入。
- 行动点与充能点集中在手牌旁，以不同形状区分，并预览技能消耗。
- 教程操作完成自动推进；纯说明点击继续。使用小纸签和手绘箭头。
- 局域网只呈现创建房间和加入房间，自动启动后台服务；地址输入折叠。
- 规则书是可随时关闭的侧边抽屉，图标和棋盘例子优先。
- 对方 pending 显示“等待对方操作”并屏蔽游戏输入，完成后恢复；自己的 pending 仍可操作。

## 当前实现与验证

- PVE 模块改为创建冒险房间时加载；资源缺失明确报错，不再在启动阶段拖垮 PVP。
- 完整资源 PVE 房间 2 项回归、缺少 PVE 资源 1 项回归通过。
- 新增棋盘原生悬停描边；对方 pending 输入屏蔽与恢复、现有渲染器共 34 项回归通过。
- 游戏窗口注入禁文字选择样式，输入与可复制元素保留；Electron 类型检查通过。
- 教程成功操作自动推进，结果留在提示回看；开场和回合复盘保留确认。完整教程 47 项回归通过。
- 教学落点及示范圆片使用 Three.js 棋盘坐标，虚线路径、脉冲和示范圆片随棋盘变换；减少动态效果设置下停止动画。部署/新棋子移动提示读取现有合法格查询，未更改规则。
- 指定技能自动打开对应角色详情，并以虚线框和箭头突出按钮；治疗→圣盾的保护配合按顺序提示。教程中不自动弹出技能语法说明，手动说明入口保留。
- 教程、棋盘动画、角色详情与页面组成共 61 项相关回归通过；对应 JS/TS ESLint 通过。独立审查未发现阻塞项，指出的合法格清空后陈旧提示已补清理。
- 本机 38673、1280×720 教程预览验证了初始教学、技能按钮提示、原详情遮挡问题；已补自动滚动并缩小纸签，最终截图确认“释放 ← 用这招”按钮完整可见。安卓尚未验收。
- 行动点与充能点已集中显示在手牌旁；无手牌和技能栏时贴底，选技能显示权威费用，取消后清除。普通回合选项不显示技能费用，对方回合仍显示自己的资源。
- 844×390 横屏网页实测发现并修复教程按钮裁切、顶部滚动条和目标提示整幅遮挡；选技能→费用出现→取消清除已交互验证。此为响应式网页验收，尚未进行安卓 APK 真机验收。
- 追加资源归属/耗尽测试通过；教程、费用、对方 pending 和技能表现 32 项回归通过，新增测试及 DOM 模块 ESLint 通过。独立审查的两项费用预览 P2 已修复并复查无阻塞。完整 battle-ui-boundary 中 widthCoverageZoom 字符串断言失败，经核实 HEAD 已存在该不匹配，未修改镜头实现绕过断言。
- 更新页、LAN 简化、规则书和整体动画节奏仍待实现；当前改动尚未打包。

2026-09-15 本轮：隐藏详情中的 divine-shield-defense / holy-shield-defense 内部被动，不改变状态效果。Windows 启动更新窗先展示并检查；主进程记住已发现更新，下载失败不能绕过；资源管理恢复入口可处理手动候选，返回首页继续检查。54项更新/页面回归与 Electron 类型检查通过，独立复审无阻塞。update-entry-smoke 因本地缺少 playwright 未执行成功；真实 Electron 恢复路径、Android 启动门禁尚待验收，未打包。

局域网入口追加合同（Medium）：用户确认覆盖 1v1 / 2v2。范围 index.html、lobby.html 与入口回归；复用 Colyseus rooms.list/create/join 和既有阵营/四席位规则，不改联机协议或数据库。创建自动准备本机服务并打开模式/地图选择；发现列表展示实际房间模式人数，手动地址折叠；校验资源后传递目标房间到大厅选择阵营。基线 ebe47eee059d18b5282856759a1a04edbc553d67，已 fetch 且 baseline 通过。验证服务启动失败、资源不匹配、2v2 路由与人数展示、过期扫描丢弃；独立审查。

局域网入口结果：创建直达现有模式/地图弹窗；局域网查询 rooms.list 展示 1v1/2v2、人数、满员/进行中，并携带房间 ID 到阵营选择；手动地址折叠。21项入口/连接/重连/真实四客户端2v2回归通过；追加房间展示与目标路由测试后入口9项通过，ESLint通过。独立复审无阻塞；浏览器已打开新版首页检查入口。尚未执行 Windows/Android 真机跨设备发现与加入，不等同完整联机验收。

规则书合同（Medium）：用户确认继续下一项。新增 data/pages/js/battle-ui/rulebook.js、css/rulebook.css 和 battle.html 入口及对应测试；顶部书本打开可关闭抽屉，主题切换，SVG棋盘例子、短句；区别PVP部署/AP与PVE规则。不改引擎/存档，范围仅表现层。依据 deployment.ts / turn.ts / terminal.ts / adventure-boundary.ts。验收关闭/Escape恢复焦点，部署6格图、资源增长、窄屏可见，独立审查。基线同上，已重新fetch。

规则书结果：六主题SVG图示抽屉接入battle顶部；PVE差异分开显示。1280×720及844×390部署页截图完整，Escape关闭并恢复入口焦点。修复对手pending锁误挡规则书与旧Escape处理冲突；25项页面/输入锁回归通过，ESLint与diff检查通过。未打包或安卓真机验收。

用户要求替换规则抽屉为一整张桌游规则页，四幅墨线插画结合大字短句（AP增长、部署与免费移动、核心死亡胜负、核心掉落充能）。仅修改rulebook.js/css；部署5格内/外待用户确认，不改变引擎。继承关闭及pending豁免。验证桌面单页、窄屏阅读、关闭与独立审查。

单页规则书已替换：四项墨线图示、2×2整页，用户确认部署仍>5；保留PVP速查标注和关闭逻辑。ESLint、pending回归2项通过，独立审查无阻塞。桌面图文完整，横屏检查后进一步压缩装饰留白与页脚；未打包。

用户追加：对局书本显示一次提示，点击后本局隐藏；教程选择页增加规则总览按钮，复用同一规则书。范围 rulebook.js/css/tutorial.html；仅只读UI，无玩法改变。房间按roomId会话保存，离线无房间ID则当前页面生命周期。验证入口打开/提示消失/仍可再次打开及选关入口。

入口验证：浏览器对局显示提示，点击规则书后提示消失，Escape关闭后不重现；选关页规则总览可打开同一四规则页。ESLint通过。

## 2026-09-15 官方转发与排位分工（替代直接承载普通房间方案）
用户已批准：普通PVP/2v2/PVE由创建者本机Colyseus承载；公网官方/非官方入口仅转发普通房间，排位由官方权威执行结算。LAN直连；本机服务常驻。风险High，沿用当前分支与用户不写Linear的决定。
base_branch: main
base_sha: ebe47eee059d18b5282856759a1a04edbc553d67（本轮fetch及baseline通过）
范围：data/pages联机/PVE入口及配置、lib/server/relay、multiplayer-relay、Linux服务脚本配置、对应tests与本合同。复用现有Colyseus和官方排位模块，不修改规则/存档/排名算法/密钥，不发布Release。
验收：官方默认/非官方手填可切换；普通创建始终本机，公开列表包含PVP/2v2/PVE；转发拒绝远程create与管理接口；真实SDK客机经转发加入PVE及2v2；本机服务在远程连接后仍运行；官方排名不接受普通房间结算。邮件用户已有，稍后本机配置，不伪造邮件验证。独立审查及候选验证后交付；安卓真机单独标记。
回退：仅撤回本轮入口与转发部署，保留现有未提交内容；服务器使用独立版本/服务，保留原rvb-game及PG备份，不能以旧版本覆盖新数据库。

本轮结果（普通房间转发）：首页公网入口改接multiplayer；默认play.redvsblue.top，可选非官方地址。聚合公开主机PVP/PVE列表，PVP人数数组和PVE人数值分别处理，满员/不可加入禁用；创建准备本机权威→发布→进入所选玩法。LAN创建广播，首页发现列表提供PVE入口。私密邀请码和直连可选PVE。PVE原生客户端新建始终使用本机，加入仍用指定远端；不改变存档与排名规则。
独立审查发现并修复玩法参数覆盖、按钮disabled恢复、PVP人数展示、发布后未导航、私密PVE误入PVP大厅。入口13项通过；既有真实转发/PVE/2v2共5项通过，官方排位14项通过；PVE两项另经生产HTTPS/WSS转发复验通过。ESLint及diff检查通过。浏览器实测官方列表返回，无公开房间时显示空态。
尚未完成：安卓/Windows客户端打包和跨设备验收；邮件尚待用户本机配置，官方排位未部署，不宣称上线；本轮没有提交/发布。

用户收口服务器页：LAN仅保留主页创建/加入；multiplayer移除LAN创建、主机直连及frp功能，简化创建弹窗。范围multiplayer.html/js及对应UI测试；不改网络协议、主页LAN、游戏规则或服务器。Medium，基线沿用刷新后的main；验证页面无失效绑定、创建/邀请码仍可用、独立审查。回退仅撤回本轮页面删改。
服务器页收口完成：创建弹窗只显示玩法、标识、公开性、创建按钮；LAN/直连/frp控件和JS绑定移除，主页LAN未改。非官方开房密钥移至非官方服务器设置；共享管理在弹窗外且仅已发布时显示。14项入口/LAN回归、ESLint通过，独立审查无阻塞，浏览器截图确认精简弹窗完整显示。未打包。

用户确认合并服务器与房间大厅：公开入口统一到包含服务器选择及房间列表的大厅，去掉服务器导航；旧lobby无明确连接参数时导向统一入口，有LAN/房间参数保留现有创建加入能力。范围multiplayer.html/js、lobby.html、official.html、对应UI测试，Medium；不改房间协议和LAN路径。验收导航仅大厅/排位、公开列表模式筛选、显式房间参数不丢失，独立审查。
统一大厅结果：公开导航合并为大厅/排位两项；服务器选择与PVP/PVE模式筛选同页。裸lobby跳统一公开大厅，显式房间/LAN链接保留；公开本机创建带lobbyContext=public避免标记LAN。删除审查发现的12位邀请码循环链接。17项回归与静态检查通过，独立复核通过，浏览器验证旧入口跳转及新版导航。客户端未打包。

LAN/PVE体验合同：用户确认LAN无邀请码；主页创建三选一1v1/2v2/PVE，发现列表四类筛选（全部+三种模式），PVE同行准备与单人流程区分。范围index/lobby/room/adventure页面、adventure lobby脚本/CSS及对应测试。Medium；不改权威、存档、规则和公网邀请机制。基线ebe47eee059d18b5282856759a1a04edbc553d67已刷新；验证模式参数、PVE发现、LAN无邀请码、单人隐藏准备席位、独立审查。

### LAN/PVE UI 本轮验证
- 创建 LAN 房间拆分 1v1、2v2、PVE；PVP 模式在后续地图加载后仍保持选择。
- LAN 发现合并 PVP/PVE 房间并支持玩法过滤；LAN 页面隐藏邀请码，房间往返保留连接上下文。
- 单人冒险入口隐藏房间列表和准备席位；组队页使用四个同行席位。公网加入仍打开公网加入弹窗。
- 17 项入口测试通过，相关 JS/测试/预览脚本 ESLint 通过。独立复审未发现新明显回归。
- 浏览器确认单人头像和入口正常、LAN 三模式弹窗居中。预览脚本补充 public/images 资源回退。
- 尚未完成 Windows/Android 实机跨设备 LAN 广播与完整开局验收；浏览器静态预览不代表实机联机通过。

### 战斗提示精简合同
- base_branch: main; base_sha: ebe47eee059d18b5282856759a1a04edbc553d67（本轮已 fetch）。
- 目标：仅主动技能/充能技能/用牌展示横幅；日志隐藏行动点、空泛触发/属性变化、用牌自身消耗。
- 范围：battle-action-history/vignette、presentation-events 与相关测试。Medium；不改规则、伤害、事件回放和存档。
- 验收：伤害死亡位移继续可见；额外弃牌可见；非主动结果无横幅。执行 UI 测试及独立复审。
- 实现验证：74 项 presentation/history/vignette 测试通过，相关文件 ESLint 通过。手动响应技能有 skillId 且未取消时保留横幅。
- 用牌消耗通过 consumedByPlay 标记区分额外弃牌；不删除底层事件。damage-buff 泛化“强化”条目隐藏，实际伤害结果保留。
- 独立审查提出过滤后聚合与点击高亮不一致，已统一过滤顺序。尚未进行本轮客户端真人对局视觉验收；需更新客户端服务端构建才能消费新的消耗标记。

### 属性变化展示修正
- 用户澄清：真实属性变化必须可见，仅隐藏无具体属性/数值的泛化强化。沿用当前 main 基线与表现层合同，Low。
- 属性事件恢复显示，中文标明攻击/防御/移动范围/生命上限、增减值以及已有的变化前后数值。不更改规则或事件来源。

### 已使用卡牌历史
- 沿用已刷新 main 基线。Medium；范围 history UI、battle.html 和 UI 测试。
- 已公开使用的卡牌显示卡名并可点击查看详情，展开/滚动旧日志仍可查看；不披露未使用手牌，不改规则。
- 已使用卡名在紧凑和展开日志均显示，可左键/右键打开详情；上拖/上滚展开历史。只读取公开card事件的cardId。
- 18项现有日志回归测试及ESLint通过。独立复审发现异步卡牌详情缓存遗漏和连续点击竞态，已补充缓存读取、请求序号与页面销毁检查。尚需客户端视觉验收。

### 训练营仅 PVP 棋子
- 沿用本轮刷新 main 基线；Low。范围 battle.html、对应 UI 测试；不改 PVE 可用范围。
- 开局和训练中放置列表只允许 ready/PVP 内容，旧无标记角色兼容；旧选择按相同条件清理。测试不同 availability 和阵营。

### 居中卡面与批量强化
- Medium；沿用已刷新主线。范围 battle.html、vignette表现层及测试；不改结算顺序。
- 日志查看已使用卡牌复用 HandCardFace 居中放大；同一行动中相邻同来源属性强化合并为一个动画节拍，其他攻击仍保留顺序。
- 独立复审两处问题已修：详情卡宽高/插图/文字覆盖旧移动样式；battle-presentation消费每个statChanged并在所有目标同阶段显示属性增减浮字。
- 41项vignette/history-board测试通过，相关JS静态检查通过。未重载用户当前训练局；刷新后可验收新样式。尚未实机验证移动端视觉。

### pending 行动链与批量 statusTag
- Medium；用户纠正批量对象为状态标签。范围 presentation-events、history/vignette 和测试。
- 使用事务标识将多次响应日志归入发起行动，保留原事件ID及各次回放；批量同源同类型状态在一个节拍播放。
- 78项相关测试通过。复审发现子响应历史快照选择和旧指针监听卸载问题，已按事件原rootEventId取快照并同步卸载处理器。
- 批量同类状态并行，日志事务归组保留底层事件ID。链标识由服务端/引擎新投影生成，需要更新引擎构建后新对局生效，旧记录不猜测拼接。

### 状态名称、训练引擎与候选标记
- Medium；范围 effects icons、vignette、renderer、原生SVG、引擎构建。增益不再泛称强化；过滤无视觉内容节拍；候选棋子增加手绘粗框Sprite。
- 必须重建浏览器引擎，验证事务关联代码进入训练运行产物。保留规则和真实攻击顺序。
- 实际运行产物已执行 build:game-engine（包含练习/PVE worker），浏览器新训练局启动成功。用户原局未重载。
- 85项日志/vignette/renderer测试通过，相关JS ESLint通过。复审提出纹理加载前白块遮挡，已改为加载成功且标记仍存活才显示。
- 候选粗框具体对局观感、图拉扬完整用牌响应仍需新局验收；不能使用旧已加载页面判断新引擎是否生效。

### 候选提示平行棋盘 / 简短增益
- Medium；用户确认候选标记必须与棋盘平行，增益只写“获得增益”，状态动画约250ms。范围renderer、vignette、presentation及测试；不改玩法。
- 用棋盘平面几何笔触替代朝向屏幕的Sprite，避免透明纹理黑块；状态增加不显示具体效果文字。
- 实现：候选提示由平行棋盘的几何笔触构成，中央留空且无需纹理；状态同批250ms，buff仅显示获得增益。
- 75项相关测试通过，相关JS ESLint通过。本轮为页面JS变更，无需再编译引擎；当前已打开页需刷新。实际候选视觉仍待用户验收。

### 统一 pending 因果日志合同
- Medium，沿用已刷新 main。用户批准所有pending按发起行动/嵌套响应/恢复原行动分层。
- 范围：presentation-recording/events、history UI、样式、相关测试和浏览器引擎构建。观察性记录，不改变结算、选择或随机流。
- 验收：触发作用域的实际效果有明确来源路径；根卡效果回根层；多次选择隐藏；嵌套响应按作用域嵌套。保留原始事件及历史定位，私密事件不泄露路径。
- 59项recording/events/history测试通过，相关ESLint通过；已重建browser/practice/adventure引擎。
- 新测试覆盖嵌套来源路径、退出响应后回根作用域，以及卡牌/响应/恢复结算DOM层级与顺序。复审遗漏的block来源已补齐。
- 旧局旧事件不补猜来源；需要新局。尚未真人完整复演图拉扬响应链。

### 候选框尺寸与高度微调
- Low；base_branch: main；base_sha: ebe47eee059d18b5282856759a1a04edbc553d67（本轮已 fetch）。
- 仅调整 renderer 候选框：按棋子宽深收紧四角，高度从 0.7 降至棋子顶面附近 0.24，保持棋盘平行；不改选择规则。
- 验收：标记贴近棋子，不包住上方生命/状态标签；运行渲染器测试和静态检查。

### 启动更新门禁补全
- Medium；沿用已刷新 main 基线。范围 startup-update-gate、official-updates 与测试。
- 更新检查失败不放行；更新弹窗内显示本机服务状态并支持重试，进入按钮默认禁用。保留资源管理恢复入口。
- 不修改安装协议、签名或服务启动规则。验证门禁测试、更新入口浏览器冒烟及静态检查。
- 验证：7 项启动相关测试、ESLint、Windows/Android 更新入口浏览器冒烟通过；新增失败后重试并进入的模拟场景。独立复查未发现本轮阻塞问题。尚未在打包 Windows 客户端验证真实下载与安装，也未复现旧版游戏准备失败的底层原因。

### 离线进入（用户修订）
- 用户允许未发现新版时检查失败离线进入；已发现新版后下载失败仍拦截。恢复门禁的离线条件，弹窗显示“离线进入”。沿用本轮基线与范围。
- 修复自动资源更新降级：bundled-base同installed参与版本比较，旧/同版在ABI检查前返回current。34项更新/门禁测试、ESLint及独立复查通过。修复已编译到win-unpacked本地验收客户端；此前发出的安装包未重打。已有安装资源需要用户恢复内置一次，不静默删除。

### 排位恢复内置后旧身份缓存
- Medium；base_branch: main；base_sha: ebe47eee059d18b5282856759a1a04edbc553d67（已 fetch）。范围 official.js、UI测试和本地验收包。
- 复现：Windows rvb-client 协议未识别为原生客户端，匹配提交旧localStorage身份。修复为每次读取本机当前profile，读取失败不回退旧缓存；不改变服务端兼容规则。
- Windows失败测试先复现，安卓作为对照；不修改账号数据或服务器。
- 验证：4项真实页面脚本VM测试、ESLint与main基线通过；已同步win-unpacked两处official.js且SHA一致。安卓防降级uiAcceptance构建成功并复制独立update-fix APK。真实账号匹配待用户验收。

### 联机验收修复（2026-09-15）
- 用户批准持续修复，无需逐项确认。Medium；base main ebe47eee059d18b5282856759a1a04edbc553d67，已fetch。范围联机页面/Colyseus客户端和房间/Relay/移动布局/资源打包及相关测试。
- 验收：LAN退出可重进，LAN与公网目录隔离；Relay PVE可加入；安卓PVE客机可出牌；房主保存解散、队员离开；房间列表可滚动、资源栏不遮部署；资源显示与操作延迟定位。
- 不改玩法、账号、排位算法、依赖或发布渠道；保留已有工作。每项最小复现测试、相关静态检查、候选构建和独立审查。部署仅限已授权官方节点修复。

## 联机边界（2026-09-15 验收修复）
- 本机权威服务独立运行，为训练、单人冒险、LAN 和自定义房间提供服务。连接官方服务器不会停掉它。
- LAN 使用专属 `lan.html`，地址来自入口显式 serverUrl。目录不读取官方服务器地址，不显示邀请码。
- 公网目录使用 `multiplayer.html`，只读取所选 Relay 的 hosts。休闲 PVP/PVE 仍由玩家主机执行规则和保存，Relay 仅转发。
- 官方排位由官方权威服务器执行规则。账号 Bearer/officialToken 与普通房间签名 admission 分离。
- 普通 Colyseus HTTP 不携带跨域 Cookie，避免 Relay 列表可读但匹配请求被浏览器 CORS 拒绝。
- 房间“离开”等待席位释放；断线保留重连窗口。旧连接断开不能清除新连接映射。
- PVE 房主“保存并解散”在同一命令队列中保存、冻结后续操作并关闭房间，队员“离开”仅离开自己的连接。

### 本轮证据与候选（2026-09-15）
- 后续反馈范围：安卓 LAN 广播与房间内 IP、单条存档删除、移除阵营白点、PVE/2v2 队友技能移动。Medium；基线再次 fetch 仍为 ebe47eee059d18b5282856759a1a04edbc553d67。不改变棋子普通控制权、阵营规则、存档格式或投降规则；验证删除权限和不影响进行中的冒险、UDP 协议及合法技能落点；只生成本地候选包。
- 51 项相关回归通过：`output/network-regression-2.log`；追加入口来源传递及资源条测试 10 项通过（包含前一组重复用例）：`output/network-final-ui-tests.log`。首次并行构建期间 cooperative 用例超时、旧 SDK mock 缺少 http.options，分别在构建结束后复测并按实际 SDK 补齐 mock，未修改规则断言或放宽测试超时。
- TypeScript 和定向 ESLint 通过：`output/network-types.log`、`output/network-final-lint.log`。独立审查确认 Relay、卡牌作用域、保存冻结和主动离开修复；公开房主返回 LAN 的审查发现已修复并补测试。
- Edge 真实跨域 Relay：SDK 默认 include 复现 Failed to fetch，omit 后 PVE 加入与快照成功；PVP 加入、读取及准备状态写入成功。证据 `output/relay-cors-review/result.json`、`result-pvp.json`。
- 手机尺寸公开列表连续触摸滑动到底部，LAN 列表最后一项可滚入视口，PVE 初始头像加载成功：`output/mobile-list-verification.log`。两个尺寸的实际战斗页面部署面板布局夹具与资源条无相交：`output/network-deployment-layout.log`。这些不等于物理安卓真机验收。
- Windows 新包资源/PostgreSQL 验证通过；隔离副本实际启动、更新检查入口、本机服务恢复、教程、建房加入、资源身份、退出清理通过：`output/network-windows-client-smoke-final.log`。旧 network-client 扩展测试仍引用已移除的 frp 控件，未把该旧脚本标为通过。
- Android uiAcceptance APK 构建成功，包内新目录、网络脚本、布局、全部队伍头像和服务端身份逐项验证：`output/network-artifacts-check.log`。当前未连接 ADB 设备，不宣称本轮已完成真机安装或双设备长局。
- 官方节点无活动对局／排队后，备份 `/var/backups/rvb/rvb_official-20260915T005940-94469.dump`，切换 `candidate-frALKn`；维护保持 false、健康正常。回退目标为先前 `candidate-MPdo4n`。部署日志 `output/network-deploy.log`。
- Windows、Android、官方服务端 resolvedProfileHash 同为 `8c4eacbece0364dd6f266f1b13065fbc56ef4c14f929d83d957db6ee3841a9ab`。本次未提交、推送、创建 Release 或发布更新渠道。
- 圣光卡牌现在仅在允许的战区选取自动目标；回归验证扣牌、扣 AP、治疗／伤害／增益及区域外对象不变。PVE 命令路径去除重复 checkpoint 校验；长局性能仍待真机测量。
- 战区投降未擅自定义。当前可通过“同行者”中的离开／保存并解散管理房间，后续需明确战区败北规则，详见 `docs/technical/NETWORK_ENTRY_MODEL.md`。

#### 2026-09-15 LAN 与友方技能补充验证
- 图拉扬 PVE 候选落点复用最终战区校验；2v2 回归证明可移动队友并穿过友方，但不能普通移动队友。2 项通过，独立审查无阻断项。
- 存档删除：PostgreSQL 集成通过；SQLite 重启、外人拒绝、旧 revision 拒绝、历史删除后不复现通过。未删除用户实际存档。
- LAN 页面 9 项、渲染 32 项通过；白点隐藏后更新对应断言。SQLite 测试包含子进程重启，超时由 5 秒调整为 20 秒。
- tsc --noEmit、脚本兼容审计、build:game-engine、main baseline 检查通过。尚未重新生成安装包或部署服务器；Android 广播仍需实体双设备验收。
- 广播同步异常与发送错误现已捕获记录，IP 页面读取失败不会中断房间连接。

- 用户确认使用点击高亮格。额外执行真实 onCellClick 函数的 PVE/2v2 分支回归，均提交 pendingTargetSelect 而非普通 move；battle-page-runtime 通过。2v2 当前未复现拒绝，不能将 PVE 战区修复宣称为 2v2 根因修复。

### 联机与存档收尾（2026-09-15）
- 同一目标继续，已重新 fetch，未切换基线。新增广播周期/停止/枚举失败恢复测试，以及 LAN IP/端口/公网隔离测试。
- network-save-verification.log：6 文件23项通过，包含真实 PostgreSQL、SQLite 保存删除和房间生命周期；lan-address-verification.log：3项通过。tsc --noEmit、build:colyseus 和现行 scripts/build-android-host.mjs 通过。最初误用旧 build:mobile-server（WebView 架构）出现 shim 导出错误；没有修改旧兼容层，切回原生 Android 权威构建后成功。
- 独立复审广播与地址展示无阻断项。新安装包和实体安卓双设备发现尚未验证；本轮未重新部署官方服务，也未发布更新。

### PR 与双端构建（2026-09-15）
用户要求开 PR 自行合并，再构建 Windows/Android。纳入当前 RED-181 累积验收改动，无 Release/自动合并。首轮广泛回归437项通过；其余为旧入口/白点/增益顺序断言、状态分类遗漏及缺少官方测试构建产物。补齐分类并更新已批准的界面断言，准备官方隔离包后复验通过；ESLint 冷启动用60秒超时复验通过。lint零错误、tsc、内容审计通过。手机双设备发现、长局及2v2原始失败仍待人工复测。

### 正式发布范围修订
用户明确要求撤销仅验收包/不发布渠道的限制：Windows、安卓、配套资源三者正式发布并写清更新内容。版本递增为0.1.5，沿用既有签名和信任策略，防降级仍保留。PR由用户合并；签名包、资源身份、服务端兼容及上传结果必须分别验证。

### 0.1.5 安装资源后本机身份读取超时
- 用户反馈更新到资源 1.0.1 后，重启仍准备失败。base_branch: main；base_sha: 2ba774706741715bc7b0dccbca57b04272291d3c（已 fetch）。风险 Medium。
- 日志显示对局服务已监听，但 /catalog/identity 在固定 3 秒截止时超时，恢复连续三次耗尽。范围仅 electron-client/main.ts 身份请求等待与回归测试；不跳过身份解析、签名和资源校验，不修改玩家数据或在线版本。
- 验收：超过旧 3 秒但正常返回的身份可启动；永久无响应仍限时拒绝；损坏和中断响应明确失败。需以已签名 1.0.1 验证实际重启，不将单元测试等同真机通过。

#### 根因排查与范围更正
- 实际 authority.log 显示手动恢复后每次均销毁并新建进程，监听后约 3 秒身份请求超时；不是同一进程首次慢、再次快。已安装签名资源的首次身份读取测得 4225ms，内置资源约 1020ms；计时涉及资源完整性及发布者验证，不涉及历史对战记录遍历。
- 修复范围增加 profile-runtime.ts 和 create-colyseus-server.ts：在服务报告 ready 前完成身份验证；复用初始恢复读取的状态，去掉一次重复完整校验，保留完整性和签名检查。客户端先检查服务入口是否存在，缺失立即失败，避免在必然失败的三次恢复里重复资源校验。
- 保留原 3 秒身份请求上限的隔离 Windows 包，使用签名资源 1.0.1 完整冒烟通过（output/identity-root-three-second-smoke-2.log）：首次准备成功；故意移走服务入口并终止进程，三次失败后停止；恢复文件后手动重新准备成功。故意移走入口是测试注入，不是断言用户安装文件缺失。
- 相邻回归：资源运行时、资源存储、归档、冒险房间和身份请求共 32 项通过；就绪顺序 2 项通过。后续增加恢复后重复准备的进程复用检查。30 秒身份请求上限只作传输兜底，不作为根因修复证据。
- 同一热修包含安卓教程提示遮挡：battle.html、tutorial.css、tutorial-lesson-runtime.js；横屏提示紧凑停靠在任务条下方，隐藏重复状态文案，明确先选中新增棋子再选择移动落点。安卓真机验证及正式热修包仍待完成。

#### 最终复审更正
- 独立审查发现复用恢复状态可能把旧资源状态绑定到另一进程更新后的指针缓存；已完整撤回 profile-runtime.ts 优化，保留原并发校验边界。最终修复核心仅为先完整验证身份再报告 ready，及服务入口缺失提前失败。
- 撤回优化后重新构建 Colyseus，隔离包仍用原 3 秒身份请求限制，完整测试通过：output/identity-final-three-second-smoke.log。包含首次准备、三次故障耗尽、手动恢复，以及恢复后并发两次准备成功且 PID 不变。最终就绪、请求、资源运行时 7 项通过；相关 ESLint 通过。
- 当前无 adb 连接设备，手机尺寸浏览器预览不能视为安卓真机通过；尚未发布新版本。
- 914×411 浏览器实操增援课程：选择吉安娜、点击合法部署格、点击移动高亮格，课程成功进入幸运币步骤，行动点仍为 1（免费移动）。紧凑提示位于任务条下方，没有截图中覆盖中央移动区域的大气泡；该结果不代表 Android WebView/GPU 真机验证。

### 启动实际进度检测（用户批准实施，2026-09-15）
- 已 fetch；base_branch: main；base_sha: 2ba774706741715bc7b0dccbca57b04272291d3c。目标：资源校验持续推进时继续等待；90 秒无真实进展或服务退出时明确失败；单次等待 15 分钟最终保护。风险 Medium。
- 范围：Electron 等待器及启动资源请求、资源校验进度观察、Colyseus 启动阶段、两个已鉴权的 content-profile 启动接口及相关测试。不修改资源校验结论、玩家存档、权限或发布渠道。
- 进度在完成每 32 个文件或最后一个文件后报告，不发送周期性心跳。资源请求使用随机请求 ID 和 AsyncLocalStorage 隔离；退出、失败和请求结束清理监听及计时器。错误保留最后阶段和原因，恢复后重建独立等待预算。
- 验收：超过旧时限仍真实推进可完成；重复/倒退进度不续期；停滞、总上限、进程退出、半截响应均失败；并发请求不互相续期；资源完整性原回归通过。
- 实施与验证：6 文件 28 项回归通过（output/startup-progress-final-tests.log），ESLint、Electron 类型检查及 Next Webpack 完整构建通过。通用校验进度不依赖 Node 模块，AsyncLocalStorage 放在仅服务端适配器，避免破坏浏览器构建。
- 隔离 Windows 包使用签名资源 1.0.1，完整冒烟通过（output/startup-progress-packaged-smoke.log）：首次准备、三次失败停止、手动恢复、并发重复准备 PID 不变及正常退出。authority.log 实际收到 384/448、416/448、448/448、签名和 ready 阶段。最终错误映射保留阶段原因；中断响应不再留下悬空 Promise。
- 独立复审发现并修复半截 HTTP 响应导致悬空的问题，最终复审无新增明确问题。上述为代码与隔离包验证，正式热修安装包未发布。
