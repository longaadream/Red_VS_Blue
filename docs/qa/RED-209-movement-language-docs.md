# RED-209 后续：位移边界与技能语言规范

用户于2026-09-14要求在刚合并的位移变更上明确框架约束，并更新语言标准、创建新PR。

- base_branch: main
- base_sha: 0744ae91ae3cacc8b03d22c6969de84044e225e3
- 关联：RED-209 / 已合并PR #189；本次为新的后续文档PR，不重新关闭原任务。
- 风险：Low，仅文档。
- 允许路径：AGENTS.md、docs/technical/ARCHITECTURE.md、ENGINE_CORE.md、SKILLCODE_AUTHORING_STANDARD.md、docs/product/SKILL_DESCRIPTION_STANDARD.md、本文。
- 目标：禁止绕过统一位移入口，准确说明生命周期内部写入边界；落地最新简短语言规范并保留RED-209语义。
- 不包含：旧资源源码覆盖、游戏规则或生成引擎改动、界面实现、发布和合并。
- 验收：接口与最新源码一致；禁止坐标直写及替换对象绕过；描述格式覆盖距离/时长/层数/倍率/关键字；不把未实现的UI和批量内容迁移写成完成。
- 验证：main-baseline、git diff --check、项目编码检查及本次文档UTF-8/相对链接检查。纯文档不运行游戏全量测试。
- 回退：撤销本次文档提交。

新Linear任务写入被自动审批拒绝，因此合同保存在仓库中；未通过其他渠道绕过写入。用户已授权的PR工作继续执行。

验证结果：main-baseline通过（behind=0）；项目编码检查1132个文件通过；本次6份文档UTF-8与38个本地链接通过；git diff --check通过。核对位置提交、flow适配和兼容teleport源码后，修正作者手册中过时的“传送不收集充能结晶”和“飞行预留阻挡”表述。没有执行游戏全量测试或宣称UI已实现。

## 用户补充要求：落地技能文件

用户要求将语言规范应用到实际棋子技能文件并更新同一PR。新增允许路径：data/skills/*.json、data/skill-keywords.json、tests/content/blizzard-range.test.mjs、本目录落地审计。风险调整为Medium（包含此前明确授权的暴风雪全图射程）。

核对144项，更新142份技能JSON、78份预览及打坐关键词。弃用的凛冬咆哮和已符合要求的率先出阵保持原样。保留最新位移代码和新说明：战刃挥刺停止位置、旋转冲刺穿越规则、双尾飞行占位取消及毒液位移后的定身。逐项对照见RED-209-skill-text-applied.json。

先验证全部144项非展示字段不变、78份预览各3个输入共234组非description输出等价；随后仅对暴风雪code执行已授权的全图range修改，避免用固定99格替代全图。其余技能code、targeting、费用和冷却保持原样。JSON直接供现役运行时加载，不依赖独立作者工程的code.js。

该数据提交未包含粗体UI、新手提示、全量技能实战或客户端发布；粗体UI的后续进展见下文。

独立只读审查已执行，修正：神威转移对象指代、双尾飞行标记可进入及旧词典冲突、骑射/旋转冲刺伤害先于位移、千鸟/战刃伤害依赖位移成功、剑心位移动词与teleport一致。独立预览对照亦未发现数值字段差异。

最终校验：144项非展示字段逐项对照，仅暴风雪range为授权的code修改；234组预览数值对照通过；暴风雪3项范围/挂起不改战场测试通过（包括超过99格地图）；项目编码检查1133个文件通过，diff空白检查通过。审阅不等于完整实战验收。

## 用户补充要求：粗体与可视验收

用户要求实现粗体并带领验收。新增允许路径：data/pages/{pieces,piece-selection,battle}.html、data/pages/js/skill-description.js、data/pages/css/skill-description.css、tests/content/skill-description-render.test.mjs。该增量为Low风险展示逻辑。

三处技能正文统一根据keywords输出strong，最长词优先，不修改原文、数值与标点；HTML全部转义。粗体继承主题颜色，兼容当前羊皮纸背景。4项渲染测试覆盖原文保留、重叠词、HTML转义和空输入，全部通过。

人工复核入口：运行node scripts/run-colyseus-pages-qa.mjs，打开http://127.0.0.1:38672/pieces.html，展开漩涡鸣人，查看沉默[1]、选择空地格（5）、打坐及下方关键词说明。此为实际图鉴候选页面；选棋与战斗详情已接入同一渲染器，尚未完成联机对局逐页验收。新手提示未实现，未发布客户端，等待用户判断阅读效果。

## 用户补充要求：新手阅读说明

新增允许路径：tests/content/skill-reading-guide.test.mjs，其余沿用粗体UI范围。Low风险。首次展开图鉴、选棋或战斗技能详情时弹出原生dialog，解释距离、持续回合、强度层数、攻击倍率、粗体及标点。仅点击知道了记录已读，三个入口共用本机localStorage；存储不可用时退化为本页记忆。保持键盘焦点在弹窗，确认后还原。已在实际图鉴打开弹窗供用户验收；2项生命周期测试覆盖重复调用、确认持久化、页面重载、Esc和存储失败。回退本次提交即可。

按用户补充要求，三个技能详情的技能标题旁常驻Tips入口。测试已覆盖已读后主动重开及防止重复弹窗；实际图鉴验证知道了关闭、Tips重开成功。6项相关测试、编码和diff检查通过。
