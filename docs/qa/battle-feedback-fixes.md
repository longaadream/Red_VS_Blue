# 战斗反馈修复合同与验证

用户已授权修复教程弹窗滚动条、部署详情头像、维伦预言名称及保密、鼬天照关键词、十拳剑冷却和镜花水月目标保密。

- base_branch: main
- base_sha: 23eb55067fb221d8b4ff3cd457d5465446b57bd7
- Linear：RED-206；分支 codex/RED-206-battle-feedback-fixes。
- 首次基线为 53c2c9ca3eef73d2158645b93138e242c225c604；首轮主分支前进13提交后快进到6f13402；追加冷却规则收尾时再无冲突快进4提交到上述基线。
- 已执行 git fetch origin --prune。独立工作区：battle-feedback-fixes。
- 风险：Medium；复用既有状态、投影和内容 API，用户追加授权将棋子技能冷却迁至所属玩家回合结束（详见追加合同）。
- 范围：data/pages 相关教程与角色详情；维伦和鼬的 data/skills；lib/game 必要的展示保密逻辑；对应测试、构建产物与本验证记录。
- 不包含：全局平衡调整、存档/协议变更、资源包准入变更、合并或发布。

## 验收

1. 教程弹窗不显示滚动条，短屏仍可读到全部内容和操作按钮。
2. 查看棋盘角色、部署候选与缺资料候选时，头像及时更新或清空，不保留上一枚棋子。
3. 维伦预言选项显示真实卡名且无内部编码；对手和观战者不能从公开日志、事件或快照读出所选手牌。
4. 天照之缚绑定天照关键词，原灼烧功能保持。
5. 十拳剑限制敌方下一回合的主动技能，更长冷却保持，后续正常恢复。
6. 镜花水月直接选择与后续选择事件保持秘密，正常公开攻击照常展示。
7. 用户追加：毒液施加的状态使用公共 `root` / 定身，不再创建 `venom-corrosion-immobile` / 腐蚀状态。两条位移技能修改归入资源包，重复施加遵循公共时长叠加；禁止普通移动、允许强制位移、持有者回合结束到期。保留旧专属规则文件供历史内容兼容，新状态不再绑定它。

## 验证计划

先添加能复现问题的规则、投影与 DOM 回归，记录修复前失败；完成定向测试、类型/静态检查和浏览器引擎构建。补充独立审查与界面检查。人工体验验收由用户完成。

Linear 首次建单因缺少外传授权被自动审批拒绝；用户随后明确授权，已成功登记 RED-206。未修改任何基线检查或资源包准入规则。

## 实现与回退

- 教程隐藏滚动条，保留溢出阅读和可聚焦容器；没有直接裁掉长说明。
- 角色面板在部署候选、资料缺失路径重新绑定或清空头像。
- 维伦三个普通圣光选项提供中文卡名，公开文本不包含选择结果，展示事件仅本人可见。
- 镜花水月 sparse skillsById 泄露已通过真实执行复现。正常/充能/中断日志带出资源定义的保密决策，展示层读取执行结果，不跨对局读取全局资源缓存。
- 十拳剑和志志雄压制设置至少1，技能冷却统一在所属玩家回合结束扣减；施放当回合也扣减，更长冷却保留。
- 回退只需撤销本次源码修改并重新构建客户端，或在资源发布流程回退原内容版本；不迁移进行中的对局。

## 资源交付边界

主分支 ADR-0033 已允许受信签名的 JSON 脚本更新。本次生成 `dist/red206-content/RED-206-authoring.rvbpack`，为当前主分支内容及本次技能/规则修复的完整作者候选，未签名、未发布。它可供编辑器导入检查；正式客户端安装应由现有发行者完成接受、签名和发布。客户端 UI/引擎修复仍需重新构建程序，不能由此包覆盖。

## 首轮验证结果（追加冷却规则前）

- 修复前：头像切换/清空、维伦选项名称、鼬冷却/关键词5条回归失败；真实镜花水月 sparse definitions 执行1条泄露回归失败；毒液公共定身2条回归失败。
- 最终定向回归：13个文件、192项测试通过（规则、保密投影、状态、部署面板、角色头像、教程运行时）。先前包含全部教程场景/回放的12文件155项检查也通过。
- `tsc --noEmit --incremental false`：通过。
- 受影响源码及测试的 ESLint：通过。
- `npm run build:game-engine`：通过；桌面、Android 引擎和练习 worker 均已重建。
- `node tests/electron/red206-ui-smoke.cjs`：通过。真实 Chromium 加载生产 CSS/角色扩展脚本的最小 DOM fixture，验证头像解码及切换、缺资料清空，1280×720和844×390窗口内布局、隐藏滚动条、长文本下聚焦最后按钮后可见。该检查不冒充完整联机人工对战。
- 截图与测量：`docs/qa/RED-206-ui/`。已检查候选头像、短屏标准说明与长说明滚动后按钮截图。
- 官方 `rvb build/validate RED-206 --channel authoring`：通过；作者包342个条目、297448字节。5个修改后的 JSON 与包内字节逐项相同，包内无 HTML/JS/CSS。
- 包 packageHash：`39a0c15dc15b565555c8fef7a75b11b3be2327d4a44c3bab3d8f31929d759c9c`。
- 归档 SHA-256：`3f2f41fd015590a76f369386de9932e70265bed4c509d156b6a5fed540414e57`；逐文件校验见 `dist/red206-content/verification.json`。
- 独立审查发现中断技能日志遗漏保密标记，已修复并增加普通/充能中断及后续目标选择回归。
- 独立复核：原 P2 已关闭，4项针对中断及后续选择的回归单独通过，未发现新可操作问题。
- 最终 `check:main-baseline`：通过，HEAD 与 origin/main 均为 6f13402fab0bee91e3cefdf1c3e011e040f6cac9；修改保存在独立工作区，尚未提交或推送。

## 人工验收

在更新后的客户端中，以双方视角测试维伦预言与镜花水月；对手仅看到秘密选择而无目标名称/头像/连线。用十拳剑命中敌方后结束回合，确认其主动技能仍处于冷却。用毒液交换或拉动敌方，确认显示“定身”并在规则规定的回合结束后消失。查看三个部署候选时逐一核对头像，窄屏教程能读完说明并使用底部按钮。

未进行客户端安装包发布、资源签名、线上部署或PR合并；完整人工对战验收待用户进行。


## 追加合同：回合结束扣减技能冷却（2026-09-13）

用户已明确授权变更全局棋子技能冷却时点，取代首轮限制。风险 Medium。重新 fetch 与 check:main-baseline 通过，base_branch: main，base_sha: 6f13402fab0bee91e3cefdf1c3e011e040f6cac9。

范围增加 lib/game/turn.ts、志志雄规则与被动描述、角色/回合/部署/交互测试、相关技术文档和构建资源候选。被动规则 limits 冷却与持续时间不变；不修改存档格式、随机算法、其他平衡，不合并发布。

验收：所属玩家回合结束、全部结束触发及状态到期事件完成后，每个正数技能冷却减1，施放当回合同样扣减；不扣其他玩家，零不变。开始阶段/部署续接不扣技能冷却；结束选择完成前不提交，序列化续接一次扣减，重复结束拒绝。普通1/2回合技能保持原使用间隔；十拳剑和压制至少1，限制敌方完整一回合，保留更长冷却；月读仍多等待一回合；重置冷却继续立即生效。

修复前新增5项回归失败，日志 ../cooldown-before.log。结果见下方本轮验证。规则详见 [技能冷却结算](../technical/SKILL_COOLDOWN_LIFECYCLE.md)。


## 本轮最终验证（回合结束扣冷却）

- 最终 base_branch: main；base_sha: 23eb55067fb221d8b4ff3cd457d5465446b57bd7。同步 RED-205 的4个发布准备提交后重新构建和测试，未改发布流程。
- 新基线24文件338项定向回归通过，包含固定种子真实动作、普通1/2冷却间隔、月读、十拳剑、志志雄、四人同盟、start/action超时、水门结束选点序列化续接、重复结束拒绝、部署续接、旧保密/UI修复与浏览器引擎。日志 ../cooldown-final-main.log。
- tsc --noEmit --incremental false 通过；受影响源码/测试ESLint与 git diff --check 通过。早期两处测试fixture类型已修正，临时基线导出目录已移出根tsconfig扫描范围。
- build:game-engine 通过，桌面、Android与练习引擎均使用本次源码。
- 完整 tests/game 首轮117文件1320项中1309通过、11失败；任务相关的毒液旧专属状态断言和志志雄描述审计已按用户决定同步，后续定向全部通过。其余9项由独立审查在6f13402隔离基线同样复现：targeting固定准备hash、AI admission、2项静态计数、Sonic关键词、zoom边界、RED-183旧页面断言、2项player-colyseus旧页面断言。未刷新这些固定快照，也未宣称全量绿。证据 ../red206-baseline-check/baseline-results.json、baseline-red183-results.json、baseline-ai-audit.json、current-ai-audit.json、baseline-evidence.json。新合入4提交没有修改这些测试或被测源码/资源。
- 初次资源改动后部署初始化遇到 PROFILE_STATE_INVALID：既有 bundled-base 指针在验证过程中更新导致；查明是 profile-store.readState 在内容hash变化时重写指针，未修改该范围外机制。指针更新后部署完整交互与新基线测试通过。
- 独立审查本轮冷却增量无须修代码的问题，独立4文件48项通过；建议的四人同盟/超时覆盖已补并通过。
- 作者候选更新到0.2.0，342条目、297462字节。官方build/validate通过，7个技能/规则JSON包内字节逐一相同，无HTML/JS/CSS。packageHash: 0dd594e31649a1152691e42606e00c62772e0c287fba3dc34517589ca3bdaab5；archive SHA-256: 2f9b4f34df335800762bcd5f3dc24cb329f71e00d71f594759e78feb28ddfe63。覆盖首轮0.1.0候选，仍未签名/发布；必须配合本次结束回合扣减引擎，不能独立安装到旧引擎。
- 人工验收建议：鼬或志志雄命中目标后，确认目标显示冷却1；目标下一回合全程不能使用技能，结束回合后降为0。普通冷却1技能使用后，当回合结束降为0，下一次自己回合可以使用。
- 源码保留在本地分支，未提交、推送、合并或发布。


## 追加：回合外响应定格与行动记录（2026-09-13）

用户明确授权：触发回合外 pending 的对方动作保持定格，任何对战界面都可查看行动记录，入口永远置顶高亮。Linear 合同已补充允许修改 battle-ui、样式、表现事件/recording、turn 的解析元数据观察及对应测试。风险 High（保密投影），不更改 pending 结算、计时、存档或网络协议。回退为回滚本次 PR 并重建客户端。

实现与验收：
- 定格使用当前观察者收到的公开动作，保留到整条响应结束；多步选择、响应权轮换继续使用原动作。跳过/倍速不清掉提示，目标点击不被拦截。取消/超时清除 pending 后释放。
- 行动记录挂到 body，固定于视口最高层并持续金色高亮。目标模式和弹窗不再强制收起；展开、查看历史、返回实时棋盘都保留最新待选状态。无记录时入口仍可见。
- 历史中的挂起动作明确标作“发起行动 · 触发响应”，不冒充已结算伤害/位移。查看历史时继续接收新状态，但不覆盖历史棋盘。
- 真实镜花水月对带飞雷神标记的友军施法，会在 beforeSkillUse 挂起。此前 sparse definitions 且未写执行日志时目标泄漏；现在仅在录制器 WeakMap 保留已解析技能的名称/保密标记，实际游戏状态与 hash 不变。对手/观众均不收到秘密目标。
- 新增失败回归2项后修复；最终30文件414项通过（../red206-pr-final-tests.log）。tsc、全仓 lint、ESLint配置检查、build:game-engine、最新 main 基线检查通过。既有9项全量基线失败仍按上文披露。
- 隐藏 Electron 窗口使用生产样式/模块，1280×720 和844×390均验证 elementFromPoint 命中弹窗上的历史入口、选点状态可展开、2.2秒后定格仍可见、棋盘目标可以点击。证据 history-results.json 与 history-*.png；运行 node tests/electron/red206-history-smoke.cjs 可复现。
- 独立审查5文件89项通过，未发现需修问题。人工建议用飞雷神/寒冰坚忍触发响应，在目标与选项界面打开记录，再关闭继续选择。

边界：只保留本次连接已接收到的转场事件；完整刷新/重连的快照不含旧动作，无法重建此前的定格或历史。本次未新增历史持久化。该 UI/保密观察修复需要新版客户端，不属于 JSON 资源包更新。
