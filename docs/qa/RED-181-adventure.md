# RED-181 首个同地图冒险候选

## 最新候选：IP 敌人、固定预告和敌方无行动点（2026-09-09）

基线 main `12a0937c3f39ca43e5cbb2f1d9a6f1e8c11ac441`。本节取代下文历史版本的短程搜索、敌方 AP 和双死神配置。

- 8 个文件、96 项自动测试通过：PVE 初始化/队长/部署/伤势/清场、固定计划、正式技能与召唤、PVP 练习、页面契约和团队规则。日志 `output/pve-roguelike/program-tests.log`。
- 固定计划覆盖四方向射击、闪避后不重新瞄准、重复 beginPhase 幂等、堵路取消、零 AP 执行限定移动、任意移动拒绝、召唤归属、区域隔离，以及同一回合进入第二场战斗仍有首轮预告。
- 整体 TypeScript 检查与修改模块 ESLint 通过；冒险 Worker 已重建。首轮合并回归中营地完整流程超过默认 5 秒，已将该长流程测试的超时设为 15 秒并用单 worker 运行；不是规则断言失败，也不作为 AI 性能测量。
- 独立只读 AI 审查通过：零 AP 特许绑定公开计划，重定向与非法落点拒绝，原生伤害/召唤事务正确，PVP 路径隔离。
- 浏览器实际验证：六名敌人在场且杂兵贴图正常；猎空从 (5,25) 移到 (10,25) 进入哨站后，玩家 1 AP，三名敌人的顺序与路径可见。结束回合后僵尸、死神和骷髅依次抵达预告位置，随后进入战斗第 2 轮、玩家 2 AP；敌方资源卡始终仅显示“按预告行动”。
- 实际打开死神详情确认：霰弹射击显示完整的 5 格射程、首个棋子阻挡、100% 攻击力物理伤害、地形阻挡与固定方向正文，以及冷却；不显示行动点。未手动通关整张地图，首领召唤与清场由正式引擎测试验证。

展开原生动作历史后，冒险面板向左让位，未遮挡日志；浏览器警告/错误日志为空。页面脚本语法、编码检查（1075 文件）与 git diff --check 通过。

最终基线检查发现 origin/main 已推进到 `895834297e3e49ebbf10b12074c08979931b6e01`，当前候选 Behind 2；上述验证只对应 `12a0937…` 基线。尚未同步这两个新提交，因此不具备合并就绪状态。

仍为单人固定地图切片；数值与敌人组合需要试玩调校。未创建 PR、合并或发布。

日期：2026-09-09。基线 main `03339a5efffde6cba4aaa53a33908db526d593e9`。
风险 High；用户已批准方案和实施。本记录属于本地候选验证，不代表产品人工验收、合并或发布。

## 验证结果

- `npx.cmd vitest run tests/pve/roguelike/adventure.test.ts tests/practice/session.test.ts --reporter=dot --silent`：20/20 通过，覆盖初始化、真实移动开战、封锁与非法事务不污染状态、一次搜索、营地、真实技能清场、伤势、原生核心胜负、冒险独立回合上限、支援绕行、跨界护盾、区外被动、回溯清理；同时包含 9 项 PVP 练习回归。
- `npx.cmd tsc --noEmit --pretty false`：通过。
- 修改涉及的规则模块、冒险模块、测试及构建脚本 ESLint：通过。
- `node scripts/check-encoding.js`：通过，1012 个文本文件。
- `node scripts/build-adventure.mjs`：浏览器专用 Worker 构建通过。
- `git diff --check`：通过。
- 详细测试日志保存在本地 `output/pve-roguelike/`。

## 实际浏览器操作

使用独立入口 `http://127.0.0.1:8876/adventure.html`，实际进入 `battle.html?mode=adventure`。原生木桌、倾斜棋盘、动漫圆片和技能面板正常呈现，没有训练工具。

实际点击验证：营地全队休整后，敌方空回合自动结束并归还行动权；猎空从 (5,25) 移到 (7,25)，不重新选择棋子即可再移到 (7,24)，行动点 2→1→0；下一回合在行囊相邻格搜索，金币 0→15、行动点 3→2，按钮变为已领取且禁用；继续向东跨入哨站，原地图显示“战区已封锁 · 消灭敌方核心”，搜索保持不可用。正式清场、原地解锁与终局由自动测试验证，未声称已手动通关整张地图。

独立审查关闭：河道支援绕行、跨界增益事务回退、区域外被动、清场后回溯残留。独立 AI 回合检查确认未激活核心不会移动或受伤；600ms 单次搜索只看到前两个候选，改为 2500ms 后同局面实际选择霰弹枪，约 2.65 秒。最终配置采用 2500ms 单决策、6500ms 搜索累计预算和 7500ms 计算后结束回合；客户端 10 秒单次等待保护。不是所有设备整回合小于 10 秒的性能保证，后续还需要多局候选版本测量。

## 体验步骤与边界

先选中队员，再移动至设施相邻格；点击地图设施标记查看操作。清除哨站后向北穿过旧桥，进入旧城核心。回营地仍是走原地图。

当前只有 32×32 单人首幕切片、固定猎空/乌瑟尔与两个死神核心、金币奖励和营地数值强化。完整三幕、迷雾、保存、联机、构筑奖励、社区内容尚未实现；刷新或关闭页面会清空本次进度。无局外奖励。尚未创建 PR、合并或发布。

重启预览：先运行 `node scripts/qa/practice-server.mjs`（只提供现有桌面资源），再运行 `node scripts/qa/adventure-server.mjs`。默认 8876 根路径进入独立冒险页。运行前用 `npm.cmd run build:adventure` 更新 Worker；`engine.js` 是构建产物。

## 记录限制与回退

自动审批拒绝向 Linear 更新新增内部代码路径、缺陷实测和技术细节，相关详细记录只保存在本地技术文档中。未尝试改用其他外部目的地发送。

回退独立冒险入口、模块及默认关闭的原生引擎接入分支即可；本次没有迁移既有存档、PVP 房间协议或依赖。

## 2026-09-09 重新同步验证

已按用户要求同步到 main `12a0937c3f39ca43e5cbb2f1d9a6f1e8c11ac441`，解决四处冲突并保留主线 2v2、观战和排位构建入口。原始 PVE stash 保留为备份，未提交、合并或发布。

同步后 6 个测试文件、76 项测试全部通过：PVE、PVP 练习、团队关系/效果/规则、战斗页面契约。页面 VM 补齐模式变量并验证冒险指令独立路由及观战只读。整体 tsc、修改模块 ESLint、编码检查（1071 个文本文件）、冒险构建、git diff --check 均通过。主线基线检查 Ahead 0 / Behind 0。证据见 output/pve-roguelike/sync-tests.log、sync-typecheck.log、sync-lint.log。

本机缺少主线锁定的 nodemailer 10.0.1 与 @types/nodemailer 8.0.1，已下载并校验 SHA-512 与锁文件一致后恢复缺失目录，不改版本、锁文件或运行安装脚本。之后整体类型检查通过。

独立审查实测新版 Recall、区内猎空被动伤害与区外被动隔离均通过。重启了资源预览服务、重建了冒险 Worker；浏览器实际通过独立入口加载32×32棋盘并进入玩家行动阶段。

## 2026-09-09 队长、行动点与部署修订

用户确认的本轮规则已落地，仍为固定阵容的单人候选。基线保持 main
`12a0937c3f39ca43e5cbb2f1d9a6f1e8c11ac441`。类型检查、受影响 TypeScript
模块 ESLint、冒险 Worker 构建、页面脚本语法检查、编码检查（1072 文件）和
主线基线检查通过，git diff --check 无错误。

规则与相邻回归共 7 文件 83 项通过（party-tests.log）；页面实点另发现动作
回执在 busy=true 时渲染，之后未刷新部署按钮。修复玩家命令与自动阶段完成时
的可用性刷新后，页面契约 45 项通过（party-page-tests.log），其中新增 2 项
覆盖两条异步路径，累计 85 项。之后再次通过整体类型检查及该测试文件 ESLint。

新增规则用真实 reducer/技能验证：探索回合数再大仍为 3 AP；每场从 1 AP
起步并封顶 10；免费部署、每回合限额、部署版本防重与非法动作回退；队长
阵亡仍可从阵亡位置部署；胜利队长 1 HP 复活、其他存活队员收回且保留伤势；
预备队营地治疗/独立数值强化；没有存活预备队时正常判负且不复活。

独立审查额外通过末幕真实核心结算：零 AP 部署、最后核心死亡正式 HUMAN
胜利、队长 1 HP、乌瑟尔 9 HP 保留、实例不重复、终局后指令原子拒绝。

实际浏览器在 1280×720 原生桌面棋盘验证：地点线绘图标可见；发现营地标记
遮挡相邻起点队长后加入自动避让，队长恢复直接点击选中。猎空从 (5,25)
实际移动至 (10,25) 触发封锁，显示战斗第 1 轮与 1 AP；点击部署乌瑟尔，
原生棋盘高亮队长周边合法格；点击 (11,25) 部署成功，仍为 1 AP，显示
本回合已部署，乌瑟尔 15/15 出现在地图。展开原生动作历史可查看移动/部署，
手记移至日志左侧，两者无重叠。战斗时手记自动展示当前据点以减少无关营地控件。

未手动通关整幕；胜负与收队由上述正式引擎测试验证。多人援军继承轮次、等待
回合及保护只有设计记录，当前没有多人加入实现。规则和详细技术记录仍留本地，
本轮 Linear 更新再次被自动审批拒绝，未换渠道发送。

## 技能文案精简（2026-09-09）

按用户要求，射击技能用现有“弹射物”关键词承载地形阻挡说明；统一预告执行时机、落空与取消条件移到“记录与规则”。抓击与召唤正文同步精简，伤害倍率、射程和召唤限制保留。仅改正文与关键词元数据，执行代码未变。Worker 构建、ESLint、页面脚本语法和 diff 检查通过；浏览器实际打开死神详情并展开“弹射物”，可见词典说明“弹射物无法穿过掩体和墙壁”。本次未重复运行规则回归。

再次按用户指定的原版地狱火霰弹枪句式精简：用“对……目标造成等同攻击力的物理伤害”，去掉100%及程序判断式正文。抓击、射击、召唤同步缩短，仅调整展示文案，执行代码不变。

## 棋盘贴花与弹窗修订（2026-09-10）

移除每 100ms 重投影的 DOM 地标、SVG 预告及大 HUD。新增原生 Three.js 平面贴花接口，图标和编号使用 CanvasTexture，移动路线/区域边界也在场景内；与棋盘共用相机及按需渲染，不通过 DOM 跟随。金币独立常驻、队伍独立底栏，地点/营地/搜索/敌情/手记使用纸页 dialog。

- 渲染与页面回归 2 文件、68 项通过；新增真实 bundled Three.js 检查平面方向/坐标、相机缩放及平移时 group 不重建、静止时无循环 RAF、共享纹理单次释放、回看历史时隐藏且重建仍隐藏、返回恢复与销毁。日志 board-ui-tests.log。
- 整体 tsc、测试 ESLint、两份页面脚本语法、编码检查（1075 文件）及 diff 检查通过。独立只读审查无阻断项。
- 浏览器实点营地地标打开弹窗，修正全局 CSS reset 导致弹窗落在左上角后，确认弹窗居中，金币在背景顶部仍可见。点击关闭和 Esc 均可返回棋盘。
- 猎空连续两次原生移动抵达行囊旁，不重新选棋。通过地点弹窗搜索，金币 0→15、行动点 1→0，弹窗变为已领取、地标显示完成。金币不足时营地 20 金币强化按钮禁用。
- 浏览器拖动/滚轮尝试没有产生可判读的镜头变化，未据此声称完成流畅度实测；相机平移/缩放同步由真实 Three.js 输入回归验证。尚未手动完成赚足金币后付费强化或通关，本轮不改变权威经济与战斗规则。
- 浏览器无错误/警告日志。仍为本地候选，落后 main 的同步事项保留，未提交、合并或发布。
