# 历史资源合入与发布继承

base_branch: main
base_sha: b0b21ae9df73b8cd7a54d96da3873f9d5bde8b5b

用户授权整合蓝染、乌尔奇奥拉、格里姆乔历史修改，并要求今后发布从最旧版本按历史累积。风险 Medium；范围为这三人的数据、行为回归与发布检查。保留当前统一位移接口和其他后续修改；不直接以旧目录覆盖当前目录。

历史来源：RED-201 changes.json（蓝染攻击5、再生1）；grimmjow-destruction-rule changed.json/RESULTS.md（独立 afterDamageDealt 自伤规则，清理四处重复自伤）。这些历史候选现由用户明确批准整合。

验收：双向再生1、零伤害不回复；破坏本能每次实际伤害自伤1、不递归、不双扣，归刃保留；蓝染瞬步与当前位移规则保持有效。

发布原则：从最早可追溯资源基线逐项核对历史修改，以最终累计结果构建；历史差异冲突必须显式解决，不可仅以 Git 当前 data 或版本号判定内容完整。保留发布继承记录，发布前对实际包运行角色行为与内容检查。未纳入项须明确记录原因。禁止覆写已发布版本。本次整合不直接变更线上资源或服务器。

整合结果：12 个资源文件记录在 config/resource-history.json，保留当前位移接口和简洁技能说明。历史回归13项与现有角色/瞬步回归共29项通过，独立审查复跑通过。旧测试修正蓝染与再生数值预期、补齐新独立规则，原自伤次数断言不降低；保留当前狩猎说明，不回滚为旧长文案。

发布前必须运行 `node scripts/check-resource-history.mjs <最终资源包路径>`，当前源码可不传路径检查。旧1.0.1实际归档被该检查拒绝；这项继承校验不能替代签名、完整性及实际结算回归。后续有意修改这些文件时，审查历史冲突并更新继承记录及测试后再发布，不允许直接重算校验值来隐藏未解释差异。
