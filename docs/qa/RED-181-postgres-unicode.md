# 中文安装路径下数据库启动兼容

base_branch: main
base_sha: b0b21ae9df73b8cd7a54d96da3873f9d5bde8b5b

用户授权修复中文安装路径兼容。范围：Electron 内嵌 PostgreSQL 启动路径、失败提示及回归测试。风险 Medium；不更改数据库格式、UTF-8 编码、资源签名或更新渠道，不删除玩家数据。

日志复现：从 `E:\新建文件夹 (4)\RED vs BLUE` 执行 initdb，post-bootstrap 阶段出现 UTF8 invalid byte sequence 0xd0 0xc2。重试重复失败。

验收：中文安装目录完成新数据库初始化、写入和重启读取；英文路径不受影响；仍验证原始 runtime 清单；异常显示可读原因。仅使用独立测试数据。

回退：撤回路径兼容改动，数据库位置与格式保持不变。

用户追加：桌面对局双方玩家卡片不得遮挡规则书及工具栏；调整桌面工具区固定预算及换行，玩家区预留右边距。手机横屏现有布局不变。

已验证：原命令中文目录复现同样错误；修复后真实数据库中文安装入口初始化、写入中文文本、新控制器重启读取通过，原数据库回归通过，共 8 项。

边界：本次兼容中文安装目录，用户数据根目录仍需 ASCII；若两者都含非 ASCII 字符，明确报错，不迁移或删除数据。目录入口需文件系统支持 junction。原 runtime 清单校验保持不变。

最终验证：Electron 类型检查与定向 ESLint 通过；启动状态及更新提示回归 35 项通过。真实页面样式的隔离布局预览在 1100/1280/1920/2560 宽度、全部工具按钮显示时无玩家卡片重叠；该布局预览未连接真实对局。独立 AI 审查无阻断问题。本次尚未构建或公开发布新安装包。
