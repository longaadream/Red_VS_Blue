# JSON SkillCode 资源包打包与发布任务

用户已明确要求保留 JSON 内 SkillCode，拒绝另做数值读取层，并要求编辑器必须支持打包发布。
本合同取代本任务中“外部包一律禁止 SkillCode”对编辑器创作与受信发布的旧限制。

- base_branch: main
- base_sha: 895834297e3e49ebbf10b12074c08979931b6e01（2026-09-10 重新 fetch 并通过 main-baseline）
- 风险：High（更新包信任、安装与回滚）。用户当前明确选择保留脚本资源包；依此执行必要的发布链路改造，不改数值模型。
- 允许范围：lib/content-pipeline 的策略、工具、安装与信任配置；编辑器导入/接受/打包/发布；相关构建配置、测试、使用说明和决策文档。
- 不修改：游戏客户端主进程、玩法实现、技能数值、存档/随机规则；不把签名视为脚本沙箱，不开放任意网站/陌生作者的脚本执行。
- 方案：增加只供本地创作的含脚本原包策略，允许编辑器导入和导出；玩家安装含脚本包必须通过真实签名校验且签名者在宿主明确配置的脚本发行者名单中。普通外部数据包与未知发行者保持原限制。保持 ZIP/JSON/哈希/引用/补丁基准检查。
- 发布：沿用已接受快照、版本不可变、GitHub Release 及补丁生成流程；检查先完成，再上传。用户亲自发布第一版，本轮不擅自向 GitHub 发布正式更新。
- 验收：真实当前原包构建→编辑器导入→蓝染攻击4改5、小乌两条规则heal 2改1→接受→签名打包→通过相同发布协调器准备Release内容→运行时安装/激活→验证实际规则数值；覆盖下一版补丁和回滚。拒绝未签名/未知密钥的代码包和被篡改文件。
- 测试：定向策略测试、工具/编辑器/安装器回归、真实内容集成及行为验证、Electron候选与独立AI审查。
- 回退：代码改动可回退；发布快照不可变；安装不直接覆盖稳定版本，使用既有候选激活与previous-stable回滚。旧客户端需先更新一次才能安装含脚本的新资源包。
- 本地记录任务，不向 Linear 外写。
