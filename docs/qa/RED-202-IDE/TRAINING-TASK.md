# 编辑器候选训练营接线

- 用户要求：编辑器必须能测试当前候选资源，不能只有禁用按钮。沿用之前“打开训练营，自己配置场景”的选择。
- 基线：2026-09-10 fetch 后 `origin/main@895834297e3e49ebbf10b12074c08979931b6e01`；继续已有 RED-202 本地候选，main-baseline 通过。
- 范围：编辑器 main/preload、独立预览适配器、worker、工作台入口、打包资源配置、相关测试与说明；不修改游戏客户端主进程和玩法引擎。
- 方案：用户点击试玩，将已接受内容经既有 authoring 打包校验冻结到内存，以受限资源协议供独立 sandbox BrowserWindow 中的现有 battle.html?mode=training 读取；复用当前训练营和引擎，不安装资源、不更改玩家 Profile、不连接联机服务器。
- 风险：High（用户创作脚本的显式本地预览）。用户已授权编辑器接口与训练营试玩；保持 Node 禁用、无 preload、独立临时 session、外部网络/窗口/权限禁用，训练营页面与引擎仅来自程序资源，作者只能供 JSON 和已校验资源。
- 关键验收：按钮可用；显示当前已接受 hash；进入场景配置并选蓝染、小乌；实战读取改后的数值与规则；重新打开读取新接受版本；未接受草稿不混入；非法/缺失资源不回退旧数据；窗口关闭释放预览资源。
- 测试：资源映射和快照单测、真实 Electron 按钮及训练营配置/行为验证、类型/lint、独立 AI 审查。
- 回退：关闭预览即结束临时 session；不写玩家存档/活动包。代码可回退到现有发布候选。
- 不向 Linear 写入；不发布真实 GitHub Release。
