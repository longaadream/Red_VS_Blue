# 内置代码 IDE：本地任务合同

- 用户要求：代码直接粘贴进编辑器，也能从文件或文件夹导入；不再要求维护独立 JS 源文件。
- base_branch: main
- base_sha: 895834297e3e49ebbf10b12074c08979931b6e01（已 fetch 并通过 main-baseline）
- 依赖候选：91b9bfd18c482cdc39d482e966ca9a9dca80ecc4，现有 RED-202 编辑器功能。
- 风险：Medium。重点是导入、草稿同步、撤销及保存不丢数据。
- 允许路径：electron-editor/、scripts/ 的编辑器构建入口、tests/electron/、本说明和编辑器使用文档。
- 实现：离线 CodeMirror 编辑器；技能 code、规则 skillCode 和卡牌 code 共用编辑体验；格式化、语法定位、搜索、折叠、缩进、快捷保存；通过原生选择框读取文本并预览后放入当前草稿。
- 持久化仍为原有 JSON 字符串，继续使用现有 revision 冲突检查。导入只读文本，不执行、自动保存或发布。
- 非目标：游戏引擎、游戏客户端主进程、AI 服务接入、玩法修改、全量源码迁移、运行时调试器、自动合并和发布。
- 验收：技能及规则粘贴→格式化→保存→重开内容一致；引号/换行/模板字符串不损坏；语法错误可定位且不会替换原文；撤销可恢复导入；文件夹导入能选择文件和明确目标字段；未知 JSON 字段保持；保存期间不能编辑丢失草稿。
- 检查：格式化/导入回归测试、编辑器 TypeScript 和静态检查、Electron 实际候选冒烟及截图、独立 AI 审查。
- 任务仅在本地记录。此前 Linear 外写被自动审批拒绝，未重试外写；最新用户方向替代独立 JS 创作流程。
