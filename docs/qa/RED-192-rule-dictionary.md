# RED-192 规则词典与公共能力验证

关联任务：<https://linear.app/redvsblue/issue/RED-192>。风险 High；实现者与独立AI审查者分开，未代替人工验收。

## 基线与范围

最初基线 `bb0a3cfcb5c64bcaf41068faf21bfe4ee0eecf02`，已快进同步至 `e9b6918080010f30f5b2fe5f535548f80c90a257`，保留 RED-187 新手教程。引擎生成文件冲突通过双方合并后的源码重新构建解决；临时stash备份保留。

统一状态添加、合并、来源撤销与到期；区分回合开始/结束；调整真实伤害、逐步向下取整、实际生命损失；提供位置变更校验；巫妖誓约正式死亡后复活。同步鸣人、血誓、纳米激素、寒冰坚忍、黑棺、无限刃等内容及词典。规则执行版本提升至 `rvb-battle-runner/v2`，旧版本权威对局明确拒绝执行，不静默迁移。

## 自动验证

| 检查 | 结果 |
| --- | --- |
| 最新基线完整 `npm.cmd test -- tests/game --maxWorkers=2` | 108文件、1240项：1235通过，5项基线已有失败，见下文 |
| 最后卡牌取整、显示文案合并与到期清理修正后的相关回归 | 5文件、80项全部通过，包含18项RED-192规则用例 |
| 新主线教程、RED-192和Node/browser候选验证 | 7文件、72项全部通过 |
| 用户批准的历史断言更新 | 3文件、19项全部通过 |
| 部署pending→继续→开始阶段 | 冷却只减少一次，登场完整结算后begin效果一次；已纳入部署测试 |
| 浏览器引擎 | 使用现有build:game-engine重建桌面及Android产物；没有手改生成代码 |
| typecheck、encoding、SkillCode语法及free-variable兼容审计、事件生产/消费审计 | 通过 |
| main-baseline | 同步后通过 |
| lint | 无法启动完整规则检查：ESLint配置引用import插件但插件不可用；未修改基线上同样失败 |

完整测试在最后两项卡牌取整及状态显示字段排除前运行；其后的80项相关回归验证最终修改。没有用旧全量结果代替最后修改的定向验证。原全量高并发曾有5项超时；限制并发后相关74项全部通过，最新完整运行不再超时。

关键行为覆盖：同效果时长累加、不同载荷/到期共存、撤销旧来源仍保留新来源到期时间、圣盾唯一；玩家状态计时；根定身与禁锢；原子换位与预留格拒绝；真伤受减伤/护盾、逐步floor、最低1与完全抵挡0；实际HP损失与过量吸血；卡牌玩家来源不反冻；鸣人开始阶段自然进度与每个分身加速、奖励一次；血誓两阶段到期；纳米激素三项相加；卍解每局次数跨复活保留；两核心同批死亡各触发裂魂、结晶、清状态恢复初始属性后复活，终局检查不提前判负。

## 基线已有失败

在独立未修改的基线工作区复现，未通过修改断言或批量放行掩盖：

1. `ai-semantics.test.ts`：已有8条准入诊断，涉及3份manifest hash与Shadow/Tails机制声明。此次只为新增正式死亡复活的巫妖誓约补metadataRequired，并将新反冻规则加入manifest。
2. `skillcode-static-audit.test.ts`：历史常量期待21项triggerSkill，当前19；期待134项skill，当前144，共2项失败。
3. `sonic-roster.test.ts`：动能说明的既有措辞与断言不同。
4. `targeting.test.ts`：既有候选集合hash已为`fc1fe4dd…`，断言仍为`efb69827…`。

因此目前不宣称所有仓库门禁通过。完整ESLint与上述基线问题仍需其所属任务处理。

## 独立审查与人工确认

独立审查提出并复核：卍解复活绕过每局限用、圣光惩击/恶魔仪式伪棋子来源、巫妖文案、撤销来源提前到期、飞行预留格被占用，均已修复。审查者未编辑实现文件。

三处历史结果变更已逐项向用户解释并获确认，见 [历史断言审核](RED-192-snapshot-review.md)。玩家可见规则详见 [基础规则词典](../product/RULE_DICTIONARY.md)。

## 人工检查与回退

建议实际操作：鸣人部署后打坐并留多个分身，观察己方开始奖励；重复纳米激素；对减伤/护盾目标施加真伤；对黑棺目标使用带土传送；让带巫妖誓约的核心死亡，核对裂魂、结晶与复活次数。

尚未进行桌面真实点击/动画验收。旧可信脚本仍可直写坐标，权威出口按同一禁锢谓词拒绝非法位置变更；新增规则应使用公共API，`beforePiecePositionChange`不能被当成旧脚本所有坐标赋值的自动代理。

回退本任务提交可恢复旧执行器和内容；不能把已按v2运行的对局无损转换回v1。未合并、未部署、未发布。
