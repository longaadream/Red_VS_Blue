# RED-213 角色平衡候选

base_branch: main
base_sha: bc76ce78e9014f50e5ef1ea445895de8979b0ea1

## 用户确认的规则

- 阿方：惊鸿掠水 AP2 / CP2；龙骧虎步 CD1。
- 麦克斯：速射能量炮本回合可再次释放一次，两次分别支付原 AP1；全速出击改为所有存活友方获得迅捷，CD1，原 AP0 / CP1 保留。
- 迅捷采用现有关键词定义：本回合下一次普通移动免费，可以叠加，不永久增加移速。
- 艾尔·普里莫：防御1，愤怒铁拳 CD1。流星腰带新增为第四技能，保留原被动、铁拳和肘击；AP1 / CD3，持续2回合，保护自身3×3内的其他友军。
- 流星腰带将原攻击交给普里莫，用他的防御和护盾结算，保留原攻击归属，不重复触发攻击方增伤。
- 流星腰带按 statusTag 计时：只在普里莫所属玩家回合结束时扣减，施放当回合不扣；之后两个本人回合结束时分别降为1、到期。
- 死神：死亡绽放 AP2 / CP1。
- 索尼克：追踪攻击保留正方向4格选敌及相邻传送，伤害改为0.5倍，每4层动能伤害+1。

## 范围与交付

风险 Medium。仅改变上述角色及必要结算，不修改随机算法、存档格式、依赖或发布机制。
本地内容候选从上一份 RED-212 候选继承，保留毒液修复及线上1.0.9的其他资源。
本任务不自动合并、发布、部署服务器或切换玩家客户端的活动资源。

## 验证记录

- 主线刷新及 `check:main-baseline` 通过，基线仍为上述 SHA。
- 14个相关测试文件共171项通过：角色费用/冷却、Max再次施放与迅捷、普里莫内容和代伤、伤害管线、触发器、效果队列事务、移动及冷却生命周期。
- 索尼克追踪攻击目标/落点/动能消费5项、动能0/3/4/7/8边界5项通过，合计定向181项。
- 代伤接口新增前3失败/1通过；原目标免疫顺序回归修复前1失败/6通过。最终覆盖真实普里莫规则、圣盾原目标排在保护者之前、AoE保护者死亡回退、不重复源增伤、环境归属、伤害后事件、原被动计数及用户确认的2回合时点。
- Max最初以可移除标签判断再次施放；独立审查发现机械支援能清除标签绕过限制。最终按当前回合的成功动作记录计次，移除标签或重置冷却后第三次仍拒绝且不扣AP；独立运行10项通过。
- 迅捷按现有关键词“本回合”有效，向2v2所有友方发放，但不擅自延长到队友下一回合。
- 受影响文件ESLint、JSON解析、`git diff --check`通过。
- `npm.cmd run typecheck`未通过：与修改前相同的12个既有诊断，位于 linear-1010-balance、alfonso-roster、colt-roster、new-role-wall-effects 的既有测试代码；本次新增引擎/测试没有诊断。较宽Sonic整文件仍有5项不涉及追踪攻击的失败，本次未改动相关规则来消除它们。
- 独立只读审查未发现剩余引擎阻塞项。原计划的Astra审查代理因线程名额限制未能启动，由未参与实现的Luna探索代理独立审查并运行定向测试；不冒称Astra审查。
- 正式内容工具 build/sign/validate/resolve/smoke(seed=213)通过。smoke为通用固定种子检查，不代表整局UI验收。
- 使用已安装0.1.11的信任配置，在临时资源库安装、激活、重新打开校验通过；12项本次资源变更和2项继承的毒液修复字节一致。没有切换真实客户端活动资源。
- Linux官方服务器引擎bundle构建及 `node --check`通过；未在真实服务器启动、连接数据库或部署。未完成真实界面联机手测，仍待人工验收。

## 候选身份

版本 `1.0.10-red213.1`。继承 RED-212 的480项资源，只变更本任务12个JSON路径（其中2个新增），共482项，保留毒液修复。

- archive SHA-256：`ee108823483265f08bd2e68e05833f89d94c5dae2b5e47c2beb3257430fbb505`
- packageHash：`24df6bd422341720a40822328fdc91c8ecc4f6b3c1c546270b23a4f46b133ac2`
- resolvedProfileHash：`aa1b4078f5ac0f9df55c50c99d54e62c9106db75b411fa1da07b736c2f861619`
- authorityContentHash：`8d293d8b1799f5631bc0d9fb9d42ad80b0331c6a25333eabe37990adfee08dda`

本地文件位于 `output/RED-213/`，包括 `.rvbpack`、`runtime/official-server.mjs`、校验证据和README。资源包可被旧0.1.11加载器接受，但新增代伤接口必须由配套权威引擎执行：只导资源不能让旧服务器或旧离线引擎实现流星腰带。

## 人工验证与回退

在候选支持的测试环境中新建对局，核对费用、冷却、速射每回合两次、迅捷移动消耗、代伤归属与持续时间，以及索尼克动能边界。
回退源码时撤回本任务提交；回退内容时恢复此前稳定资源。旧对局不热迁移。
