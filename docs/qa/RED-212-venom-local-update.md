# RED-212 毒液本地资源修复

日期：2026-09-23。角色：实现者。风险：Medium。
base_branch: main
base_sha: bc76ce78e9014f50e5ef1ea445895de8979b0ea1

## 复现与根因

只读核查服务器的本次对局记录，seed=1112212747：

- 权威版本30、46的共生拖行都命中相邻敌人，行动日志明确记录“相邻敌人不能成为共生拖行的目标”。旧脚本在施加定身前提前返回。用户批准相邻命中不移动、正常定身1回合。
- 第7回合、权威版本45，利爪撕裂造成的致命伤害触发对方艾露恩的守护。伤害管线正常返回 blocked=true、success=false、damage=0；技能直接复用 success 导致整次动作拒绝，保护消耗、回血、费用一起回滚。错误数字是0，并非字段缺失。
- 这不是从版本号推断的故障。main 的版本字段仍为0.1.10，v0.1.11多一个发布准备提交，但两者相关引擎及毒液代码一致。本次不处理发布提交同步。

## 修改

- 利爪撕裂将明确的正常伤害阻挡视为已施法，阻挡信息不再被包装成服务器拒绝；实际伤害仍由原伤害管线结算，异常继续抛出。
- 共生拖行相邻命中跳过位移，进入公共定身逻辑；不产生位置变化或沿途拾取事件。非相邻路径、地形与友军阻挡规则不变。更新说明和预览文字。
- 新增无触发器mock的权威动作回归，旧毒液测试仅更新相邻行为断言；没有改动其他角色或公共引擎。

## 验证

- 新增回归修复前：6失败、2通过。修复后：8/8通过，覆盖艾露恩守护消费和回血、圣盾消费、四方向相邻定身、正常伤害和远距离拖行。
- venom-resource-update、damage-pipeline、position-contact、spatial：4文件62/62通过。
- 较宽组合 venom-resource-update、venom-skills、projectile-trace、damage-pipeline：75通过、4失败。3项毒液/水门旧元数据断言在修改前已失败；另1项是未修改的死神死亡绽放旧描述断言。本次未改无关断言以制造全绿。
- TypeScript：未通过。12个诊断位于未修改的 linear-1010-balance、alfonso-roster、colt-roster、new-role-wall-effects 测试；最终没有本次修改文件的诊断。没有宣称全仓类型检查通过。
- 受影响测试ESLint、git diff --check、main基线检查通过。
- 独立AI审查无阻塞项，独立运行新增8项回归通过。
- 正式内容工具 build、sign、validate、resolve、seed=1112212747 的 smoke 均通过。smoke 是通用固定种子检查，不冒充截图整局回放。
- 使用已安装0.1.11的内置资源和信任配置，在临时资源库通过现行正式安装器安装、激活、重新打开校验；两技能安装字节与修复源码一致。未操作真实玩家活动指针，未启动真实界面完成手测。

## 本地交付

文件：`dist/RED-212/RED-212-venom-local-update.rvbpack`
版本：`1.0.10-red212.1`（本地候选）。

以已经过签名校验的线上1.0.9完整包 `content-test-bef3e77a7dc14a603e528224bb07f1b141be8cb60b523c8e71373316572fa2c6` 为资源基线，保留480个资源文件，仅两个毒液JSON发生变化；不把主线内容覆盖到已有线上平衡调整。

- archive SHA-256：`d6c52a8115c3d6bf8589791873fcb79af17834043a62e14e6fedd18b94705468`
- packageHash：`2103a9bde1961950f3750ff534239df42ae93721e19b1bb7be10e04ab6c5a585`
- resolvedProfileHash：`715cfc04d1f56b628a68749f10ed0aa6bccff4d22bed87e60fd2c693f29480a4`
- authorityContentHash：`a27df5cc8563d77f6cc89381ded31982d3ea4341e6fb1997b8c9d083b6e52de9`

沿用既有受信签名身份；没有变更信任配置或发布。详细本地证据：`dist/RED-212/install-verification.json`、`payload-verification.json`、`output/validation/RED-212/`。

## 手动验证与回退

1. 在0.1.11测试客户端的资源包管理页导入候选，查看身份后应用；退出旧对局并重新创建本地测试对局。
2. 共生拖行命中四方向相邻敌人：原地定身1回合，扣1AP并进入1冷却。隔开两格以上时确认拉到身前并定身；墙或友军阻挡仍为空发。
3. 利爪撕裂攻击带圣盾目标或触发艾露恩保护的目标：无服务器拒绝，保护正常消耗，回血按原规则发生，费用和冷却只结算一次。
4. 回退时在资源管理中恢复此前稳定资源；旧对局不热迁移。源码回退只撤销本次两个技能及相关测试/文档。

服务器尚未部署本包，因此线上对局仍使用旧规则。候选与线上资源身份不同，不能把“本地导入成功”当作“线上已经修好”；联机测试需要服务器和参与客户端采用相同候选。用户本次只授权本地更新包，没有部署、重启、合并或公开发布。
