# 动作动画顺序修复

## 2026-09-08 状态与地格批次补充

按用户追加授权，增加显式 FIFO `context.presentationQueue`，支持状态/地格效果的添加、移除四种表现批次；规则层的四类 EffectBatch 与生命周期顺序保持原样。实现边界和使用方式见 [ADR-0032](../decisions/ADR-0032-status-tile-presentation-batches.md)。当前接入天照之炎、因陀罗之矢、加具土命、赐福、混沌控制；其他内容不自动推断批次。

真实天照九格同批、混沌控制两目标四状态同批、加具土命移除后再加状态、瞬态地格先增后删、无半批渲染、被动隔离及原有三段/连锁回归通过。新增后最后运行 recorder + sequential playback 共21项通过；另 vignette 29项通过。浏览器/动态技能差分与运行矩阵5文件40项通过（与前述部分重叠），相关扩展8文件163项通过。

扩展检查的3项失败（skillcode-static-audit 两个旧内容数量断言、sonic-roster 一个动能词条断言）已在干净同SHA main复现完全相同错误。typecheck、定向 ESLint、编码检查、git diff --check、main基线检查通过；浏览器和practice引擎已重建。独立AI审查无阻断；旧 ruleTriggerSkill 入口暂不提供新queue，当前迁移内容均为主动技能。

本地预览使用训练模式及仅本地开发服务注入的测试场景。先释放天照观察整片出现，再开启须佐能乎解锁加具土命验收整片移除。2026-09-08 用户明确反馈「验收通过了」，并授权创建 PR，由用户执行合并。临时验收服务和脚本不进入 PR，未合并发布。

- 授权：2026-09-08 用户要求不同类型、连锁和多段动作分开播放；同类型才允许同时播放。
- base_branch: main
- base_sha: b8a26a90fc591b61fef9fa33db4e679fb75eb33b（已 fetch，基线检查通过）
- 关联：RED-165、RED-167；本轮授权取代原先根动作 1600ms 上限，保证多段完整顺序。
- 风险：Medium。实现者负责规则边界的只读表现采样、事件投影、播放队列和棋盘动画接线。
- 范围：lib/game 的表现事件与只读采样接线、data/pages 战斗表现、对应 tests 和本文档。
- 最终入口检查追加：lib/practice/session.ts 的真实提交入口也包裹同一只读记录器，AI 搜索试算不包裹；同步构建练习 worker 与补充真实三段技能测试。
- 非目标：规则数值、随机算法、存档或状态哈希、网络命令语义、合并发布。
- 验收：葛力姆乔移动后攻击分开；龙巢闪三段各自扣血；同一批同类范围结果允许并行，不同批次不能合并；略过、2×、回合切换、重连仍安全收束。
- 测试：先建立失败队列用例；真实技能和被动结算、表现状态/hash 中立、队列假时钟和棋盘接线回归；类型、定向静态与编码检查；独立审查。
- 回退：整体回退本轮表现记录与队列接线，不修改或重放已提交规则。

## 实现与验证

- 显式伤害/治疗批次保持身份；同类型同批次可同时演出，其余事件和多段攻击逐条播放。根动作的历史归属不变。相同 sequence 的不同根保持权威到达顺序。
- 真实提交按需采样位置、HP、登场快照和可见状态，过程只存在进程内 WeakMap；模拟/目标查询不启用。悬挂动作的试算和被阻止召唤的局部事务均撤销对应采样。
- Three.js 动画与飘字消费同一段事件，棋盘保留临时显示模型。权威状态、操作确认、计时和历史继续即时更新；不使用动画计算游戏规则。
- 葛力姆乔真实响应：敌人移动 → 被动 → 葛力姆乔移动 → 敌人 HP17 → 自身 HP9 → 敌人 HP14 → 自身 HP8。龙巢闪：HP20 → 17 → 14 → 11，三个独立 batchId；启用采样和未启用时 state/hash 相等。
- 12 个定向文件 176 项通过；后续补充了回合切换与多根补帧组合用例，相关 3 文件 52 项通过（计数重叠）。另 12 文件 130 项教程、引擎浏览器差分、房间效果链、公开投影与渲染回归通过。
- `npm.cmd run typecheck`、定向 ESLint、`npm.cmd run check:encoding`、`git diff --check` 通过。
- `node scripts/build-game-engine.js` 已重新生成浏览器与 Android 游戏引擎脚本；包含同一记录接口。新增模块和生成的 `data/pages/js/game-engine.js` 均属于本轮范围。
- 3 项已有失败：battle-page-contract 两项及 battle-history-board 一项缺少 PRACTICE_MODE 测试全局变量。在干净 origin/main 同 SHA 工作区复现完全相同错误，未改写无关测试。
- 独立 AI 审查提出的回合切换/合并补帧倒放、登场快照、逐个状态、次数映射、局部回滚均已修复；最终复审 7 项组合测试通过，无剩余已报告阻断。

## 人工体验步骤与限制

在本分支运行客户端，分别使用剑心龙巢闪，以及移动敌人触发葛力姆乔狩猎本能并选择相邻落点。确认每段移动/攻击/自伤独立显示、血量逐次变化；再切换 2×、连续略过、切回当前玩家回合，确认最终状态一致。范围伤害仅同批同类可同时播放。

浏览器验收已由用户确认通过；尚未进行本轮真实 Electron/Android 设备的触摸与帧率验收。当前进入 PR 准备阶段，未合并、未发布，未改写 Linear 历史合同。
