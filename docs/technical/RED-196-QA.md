# RED-196 候选验证记录

## 范围与状态

官方 Colyseus 权威、独立 PostgreSQL 邮箱账号、系统匹配 1v1 Elo、赛季/历史、Windows SMTP 配置与管理入口。P2P/玩家主机转发仍是休闲，2v2仍为休闲和活动。用户已选择现有QQ邮箱，实际授权码只在本机填写。

本文件记录实现者验证，不代表人工验收、已发布或公网测试完成。真实 SMTP 送达与公网 HTTPS 尚未验证。配置上限10局，不声明可同时容纳100名参赛玩家。

## 已执行的聚焦验证（同步主分支前）

- 实际 PostgreSQL/Colyseus：注册验证/过期与次数限制/找回撤销会话、账号与席位防伪、匹配容量/取消、唯一终局Elo结算、掉线后新登录归位、非ASCII管理令牌、开局取消竞态、恢复失败拒绝启动、恢复后结算再重启不重复、失败停服清理。
- `npm.cmd run test:multiplayer`：19文件、55测试通过；日志 `dist/multiplayer-qa/red196-network1.txt`。包含9项官方集成和2项Windows检查。Windows测试随后加强退出码及数据库退出断言。
- 类型检查通过；修改文件静态检查通过（types6/lint3）。
- Chrome实际页面：两账号登录、光/暗阵营、匹配、8枚阵容、战场、暗方退出账号重新登录重入、实际认输、1016/984 Elo及历史通过；浏览器截图在 `dist/multiplayer-qa/red196-official-*.png`。
- Windows包实际启动，DPAPI往返与明文不落盘，死锁并发回收仅一进程取得权限、管理命令不重启PG。仅使用隔离数据目录与虚拟邮箱。

## 逐档本机负载

Windows，AMD Ryzen 9 9955HX、32逻辑CPU、约15GiB内存，Node24.13.1。客户端SDK与服务器同一进程，数据库另进程；数字不能直接映射到云服务器配置。

`node scripts/run-official-load-smoke.mjs`：5局/50连接与10局/100连接（每局2参赛+8观众），突发重同步、实际认输结算、双人断线后AI至少8次权威命令与重连收回控制，结束后释放全部比赛名额通过。详细结果 `dist/multiplayer-qa/red196-load.json`。

同步main后的10局一档：测试进程RSS约380MiB，重同步P95约967ms，指令回执P95约388ms，15局累计结算（red196-load4.txt）。未覆盖跨省网络、持续完整棋局、SMTP吞吐或长时间多人AI同时计算，不能据此承诺公网容量。

## 独立审查与回退

独立审查已修复：官方动作误走本地签名、管理命令误触PG、启动锁回收竞态、非ASCII令牌崩溃、掉线预约与登录撤销、暗方重入、开局取消竞态、恢复失败占位。最后追加验证落盘失败仍关闭监听和PG。AI规划恢复耐久状态的技能缓存，未修改随机算法或卡牌数值。

保留RED-193 RC2；官方库与玩家库分开。停服后完整备份用户数据目录和匹配的服务包；不得用旧版本覆盖新库。合并、发布和大范围发放仍需人工验收。

## 最终候选

已通过merge同步012e595fe2affb29725c45df0c2cd819704768a0；同步提交fee8d6e9dfb95499f567f8768107c3fbd05fe2ce。main基线检查通过，behind=0。

同步后：
- 合并针对性测试：7文件82项通过（red196-merge-tests.txt），覆盖队伍效果、展示批次、顺序播放与练习。
- 完整联机：19文件55项通过（red196-network2.txt），含9项官方集成和2项Windows测试。Windows检查已明确断言赢家正常exit0、PG停服日志与postmaster.pid删除。
- 真实Chrome完整排位/暗方重登/结算通过（red196-browser6.txt）；截图已人工可视核查Elo、排行榜和历史。
- 类型检查通过（types7），编码检查1036文件通过（encoding1）。独立AI最终复核无新增阻断。

用户进一步要求游戏式布局，并明确默认房间列表。已重构主菜单入口、房间大厅、服务器浏览器和排位备战，采用固定导航、列表/详情、弹窗及常驻匹配栏。具体参考与流程见启动说明。

新增验证：
- browser12：真实QQ首次配置页面（未提交邮箱或发信）、4个静态资源可用、配置文件及无效令牌请求拒绝；真实账号/匹配/对局/重登/结算继续通过。
- 隔离浏览器中的显式UI fixture覆盖房间模式/状态/搜索过滤、选中详情、私密目录过滤、满员房原玩家返回、邀请码弹窗接续；截图名称含room-browser，房间名标记“测试场景”，不代表线上房间。
- 大厅及入口/地图/阵营回归：5文件43测试通过（red196-lobby-tests2.txt）。修正旧测试VM缺少URL全局及禁用属性顺序脆弱断言，没有修改阵营规则。
- 新服务包Windows实际启动/DPAPI/单实例/管理/停服：2测试通过（red196-windows3.txt）。
- 类型、受影响文件静态和编码通过（types9/lint6/encoding3）。独立复核关闭邀请码模态遮挡、满员房原玩家重入及首次本地身份缺失问题。

候选包从最终干净提交构建；包、commit与SHA256以候选目录release.json为准，打包后的客户端验证记录随候选提供。真实邮件/公网测试仍未完成。

## 2026-09-09 本机运营面板增量（rc2）

本次用户批准补充本机运营面板，使用独立回环端口与Windows保护的随机凭据，不新增数据库表、不改变胜负或Elo规则。保留rc1作为回退包；正常停服后可恢复rc1，用户数据目录应先完整备份。

- 官方排位9项真实PG/Colyseus回归通过；新增面板5项真实PG测试和2项Windows包测试通过。覆盖缺失/非法令牌、来源与Host、请求大小、敏感字段排除、真实账号搜索、封禁撤销会话/排队、赛季与停服保护、SMTP错误脱敏及检查限流。
- 独立审查发现浏览器在停服准备阶段断开会遗留半关闭服务，已修复，并在真实数据库回归中中断HTTP验证仍恰好一次执行关闭。
- `npm.cmd run smoke:official:panel`：实际打包node/server、Windows加密入口和重新打开、玩家端口不暴露面板、Chrome渲染、取消/确认维护、切换赛季、SMTP失败恢复、审计、390px布局、面板停服与PostgreSQL进程退出通过。
- 截图 `red196-control-panel.png` 为真实隔离测试服务器，账号和对局均为0；未虚构线上数据。实际邮件发送为0，未使用用户QQ授权码。
- 证据位于候选目录及 `dist/multiplayer-qa/red196-panel-*.txt`。真实邮件送达、公网HTTPS和跨地区持续试玩仍待人工环境验收。

## rc3 终局房间目录修复

试玩发现游戏结束后大厅仍显示“对局中”。失败回归已确认：真实Colyseus终局已将房间投影为finished，公开目录仍返回该房间；大厅状态分支又把finished误映射为对局中。

修复公开可玩目录及旧服务器客户端兼容过滤，不删除终局、战报或积分，不强制断开已有观战。选中的房间结束后同步清除其详情操作；未知状态不再显示为对局中。真实终局测试验证详情finished、目录移除、双方连接仍保持与终局持久化；PG重启回归验证目录不复活、实时详情404、参赛者战报仍200。浏览器验证实际认输、Elo与历史和旧服务器终局场景。候选证据见 red196-ended-*.txt，原rc2保留用于回退。


## rc4 管理功能扩展（PVE 延期）

用户批准扩展管理操作，并明确PVE待其完成后再接入、禁止管理员手动修改Elo。本次新增踢下线、排位资格/冷却、异常对局作废、持久容量、公告、SMTP在线编辑、本机校验备份与恢复。沿用本机管理隔离，不新增公网后台或PVE代码。

验证证据：
- 完整联机23文件69项通过（red196-ops-network.txt），包含真实Colyseus踢下线/重新登录、冻结后旧player/timer/bot队列写入拒绝、作废不计分及重启不恢复、终局保留正常结算、原P2P/转发/2v2/观战/重连回归。
- 实际PG管理API覆盖权限、来源/Host、账号与资格、容量、公告、重复作废和无手动Elo接口；新增SMTP替身覆盖先验证保存后替换、在途邮件和失败保留。
- 本机备份测试覆盖逐字节/目录校验、损坏/越界路径拒绝、恢复中断回退、活PID拒绝和真实已退出PID恢复；不删除失败副本。
- 实际Windows包故障测试通过（red196-ops-faults.txt）：阻塞恢复提交时玩家端口未开放、旧session已撤销、强制维护；HTTP abort后触发停服并释放复制阻塞，PG和listener不遗留。
- 实际Chrome包面板通过（red196-ops-browser.txt）：容量10→3、公告发布、实际PG备份→容量7→恢复3、维护保留、SMTP失败、操作审计、390px布局和安全停服。截图red196-control-panel-management.png为隔离测试环境。
- 最终typecheck、全量lint、编码1049文件通过。独立审查已关闭提前开放恢复库、HTTP中断与停服交错、首次停服失败遗留、陈旧PID阻塞回退等问题，最终无剩余阻断。

候选构建以最终干净提交及release.json为准。RC3和升级前数据备份保留；不自动清理备份或手动改变历史积分。候选打包后的实际客户端/服务验证日志附候选目录，真实SMTP送达、公网HTTPS与跨地区批量试玩仍待人工环境验收；未合并或公开发布。


## 2026-09-09 真实排位赛前流程（源码验证）

基线origin/main 03339a5efffde6cba4aaa53a33908db526d593e9 已合并到当前开发分支（56e9977）。新增真实ranked-match页面及原piece-selection页面的排位适配，服务器保存秘密禁图、池快照、绝对期限及阵容草稿。未生成或发布新的分发包，原RC4仍保留。

- 实际Chrome双账号：登录→匹配→秘密禁图→服务器抽图→原选人→查看地图并恢复8枚草稿→原渐进部署/战斗→认输→Elo/历史，完整通过；测试发送真实邮件0封。
- 4项纯赛前规则测试与6项真实PG管理测试通过；官方PG/Colyseus14项回归通过。覆盖四张地图实际创建渐进部署、秘密投影、局内池快照、版本不符拒绝排队、绝对期限重启/随机补齐、退出只计分一次。
- 增加交接中断场景：落盘version0后将赛前状态模拟为未确认starting，重启仍恢复原种子、地图、阵容且只存在一个权威房间。定向测试通过。
- 普通product-room、battle-room、spectator-room及客户端Colyseus路径4文件26项回归通过。类型、全量lint及编码检查通过。
- 独立审查发现并修复草稿恢复后确认按钮/计数未刷新、查看地图前漏保存、HTTP托管页旧profile缓存无法重新匹配；最终未报告剩余P1/P2。
- 实际截图：dist/multiplayer-qa/red196-ranked-veto.png、red196-ranked-map.png、red196-ranked-roster.png、red196-official-battle.png。日志ranked-browser.log、ranked-tests-rerun.log、ranked-handoff-test.log、ranked-transport-tests.log、ranked-flow-types.txt、ranked-lint.log、ranked-encoding.log。

验证范围为本机源码服务与隔离数据库；未触碰用户现有QQ邮箱凭据、真实账号或运行中的win-x64服务。四图结构适配通过，不代表竞技胜率平衡已获得真人数据验证。未宣称公网/真实SMTP/新版分发包验收完成。
