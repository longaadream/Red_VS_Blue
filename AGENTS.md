# Red VS Blue：AI 开发规则

## 1. 项目目标

本项目是一款策略、卡牌与经营类桌游电子游戏。

当前目标是：

- 完成结构稳定、可以邀请玩家测试的 Demo。
- 优先保证核心规则正确、运行稳定和方便调试。
- 改善主要 UI 界面和玩家操作体验。
- 为后续公开测试和正式发行建立可持续开发结构。

当前处于重构阶段。旧代码可以用于理解玩法，但不得默认旧实现仍然是正确设计。

## 2. 工作依据

开始任务前，必须按以下顺序读取：

1. 本文件 `AGENTS.md`。
2. Linear 任务合同及验收标准。
3. 与任务相关的模块文档。
4. 与任务相关的 ADR 或技术决策。
5. 受影响代码及其现有测试。

不要默认读取整个仓库或全部游戏设计文档。只读取完成当前任务所需的最小上下文。

如果 Linear 与仓库文档冲突，必须指出冲突并暂停相关修改，不得自行选择其中一个版本。

开始实现前必须执行 `git fetch origin --prune`，并在 Linear 任务合同记录 `base_branch: main` 与当时 `origin/main` 的完整 `base_sha`。不能使用未刷新或来源不明的本地 main 作为开发基线。

## 3. 开发前置条件

任务至少包含以下信息后才能开始修改：

- 明确的目标。
- 本次修改范围。
- 明确不处理的内容。
- 可观察、可验证的验收标准。
- 风险等级：Low、Medium 或 High。
- 允许修改的文件或目录。
- 测试要求。

缺少目标、范围或验收标准时，只能分析代码并提出问题，不得开始修改。

## 4. 开发前必须先汇报

首次回复必须简要列出：

- 对任务目标的理解。
- 预计修改的模块和文件。
- 主要风险。
- 准备执行的测试。
- 尚未确认的问题。

如果任务信息完整且修改范围明确，可以在汇报后继续实现；如果存在会改变结果的关键歧义，必须等待人工确认。

## 5. 修改范围

- 只修改完成当前任务所必需的文件。
- 不得顺手重构无关模块。
- 不得修改任务中标记为禁止修改的路径。
- 发现范围外问题时，只记录问题并建议创建新的 Linear 任务。
- 不得覆盖或删除其他开发者尚未提交的修改。
- 不得擅自更新依赖、存档格式、核心经济规则或随机算法。
- 文档与实际行为发生变化时，必须在同一个 PR 中更新相关文档。

如果任务提供了 `allowed_paths`，所有改动必须限制在这些路径中。

## 6. 架构原则

尽量保持以下边界清晰：

- 游戏规则层：回合、卡牌、资源、胜负和状态变化。
- 表现层：React/Next.js 页面、组件、动画和视觉效果。
- 桌面系统层：Electron 窗口、文件系统、进程通信和系统能力。
- 持久化层：设置、存档、读取、迁移和版本兼容。
- 数据层：卡牌、事件、数值配置和静态内容。

必须遵守：

- UI 不得复制或重新实现核心游戏规则。
- 公共类型、游戏事件和命令应集中定义。
- 规则层应尽量可以在无窗口环境中独立运行和测试。
- 随机行为应使用可注入、可记录的种子。
- 时间、存储、网络和系统能力应通过明确接口访问。
- 存档必须包含格式版本，不得静默破坏旧存档。
- 不要为了完成局部需求创建新的全局状态来源。

如果现有项目暂时不满足这些原则，不得进行未经批准的大规模重构；应先说明问题并提出最小迁移方案。

## 7. 调试优先原则

本项目当前最重要的工程目标之一是提高问题定位效率。

修复 Bug 时必须尽量完成：

1. 记录可以稳定复现问题的步骤。
2. 找到问题发生的最小状态和输入。
3. 在修改前建立失败测试或回放场景。
4. 修复根本原因，不只隐藏错误表现。
5. 验证修复没有破坏相邻规则。
6. 保存必要的日志、状态差异、随机种子或截图。

关键日志应包含：

- 当前游戏阶段或回合。
- 执行的玩家或实体。
- 输入命令。
- 随机种子（如果相关）。
- 修改前后的关键状态。
- 错误原因和调用位置。

禁止：

- 吞掉异常后继续运行。
- 使用没有上下文的临时日志。
- 只靠反复点击界面判断规则是否正确。
- 通过重复运行掩盖不稳定测试。
- 在不知道原因时直接删除失败逻辑。

临时调试代码必须在提交前删除，或者明确置于受控的开发调试模式中。

## 8. 游戏规则要求

游戏状态变化应尽量由明确命令触发，并产生可观察结果。

重要规则必须检查相关不变量，例如：

- 资源不能在没有规则允许的情况下变成负数。
- 同一个效果不能被重复结算。
- 已失效或不存在的实体不能继续行动。
- 非法操作必须被拒绝，并且不能污染游戏状态。
- 相同初始状态、随机种子和命令序列应产生相同结果。
- 保存并读取后，关键游戏状态应保持一致。

修改游戏规则、数值、卡牌效果或胜负条件时，必须指出受到影响的规则和测试场景。

## 9. 测试要求

运行测试前，先读取项目实际存在的 `package.json`、脚本和相关说明，不得虚构测试命令。

验证顺序通常为：

1. 与修改直接相关的最小测试。
2. 类型检查和静态检查。
3. 受影响模块测试。
4. 游戏核心流程或固定种子模拟。
5. 必要时启动 Electron 应用进行冒烟测试。

Bug 修复应优先增加能够复现问题的回归测试。

如果项目缺少自动测试：

- 明确说明当前没有对应自动测试。
- 提供可重复的手动验证步骤。
- 记录期望结果和实际结果。
- 不得把“没有运行测试”写成“测试通过”。

不得擅自更新失败快照。快照变化必须说明原因并经过人工确认。

## 10. 风险规则

### Low

文字、文档、非关键 UI、小范围独立逻辑或简单修复。

要求：

- 完成相关检查。
- 提供基本验证证据。
- 经人工批准后可以合并。

### Medium

游戏规则、数值、交互流程、公共接口或依赖变化。

要求：

- 完成相关自动测试或可重复回放。
- 进行独立 AI 审查或人工代码审查。
- 提供关键状态或界面证据。

### High

存档格式、经济系统、随机算法、跨模块架构、更新机制或安全相关代码。

要求：

- 修改前必须获得人工方案批准。
- 必须提供回退方案。
- 必须执行候选版本验证。
- 未经人工批准不得合并或发布。

## 11. Git 与 PR 规则

- 每个任务使用独立分支，并从已刷新且最新的 `origin/main` 创建。
- 分支名称应包含 Linear 任务编号。
- 在分支创建后、每天首次继续任务时，以及提交 PR、请求 QA 或人工验收前，必须运行 `npm.cmd run check:main-baseline`。
- 个人独占的短期分支可以在工作区干净时 rebase `origin/main`；多人共享或被其他分支依赖的分支不得擅自改写历史，必须由分支负责人明确选择 merge 策略。
- 同步 main 后必须重新执行任务合同要求的测试，旧基线测试结果不能代表同步后的分支。
- 提交信息应简洁说明实际变更。
- PR 必须关联 Linear 任务，例如 `Fixes RED-123`。
- 不得直接推送到 `main`。
- 不得绕过分支保护或失败检查。
- 不得自行合并 PR。
- 不得自行发布版本。
- 不得修改密钥、令牌或其他敏感配置。
- 不得使用破坏性 Git 操作覆盖他人的工作。

PR 中必须说明：

- 修改目标和范围。
- 验收标准完成情况。
- 风险等级。
- 测试步骤和结果。
- 截图、日志、状态差异或其他证据。
- 已知问题。
- 回退方式。

## 12. AI 角色分工

AI 在每个任务中只能承担明确角色：

### 实现者

- 根据任务合同完成最小范围修改。
- 添加或更新测试。
- 提交 PR 和自检报告。
- 不得自行宣布需求已经通过人工验收。

### 审查者

- 根据原始任务和验收标准独立审查。
- 优先检查错误、回归、范围越界和测试缺口。
- 不得只复述实现者的说明。
- 必须引用具体文件、行为或未通过的验收项。

单人开发时，开发者和人工验收者可以是同一个人；Medium 或 High 风险任务仍应尽量由另一 AI 执行独立审查。

## 13. 完成定义

只有同时满足以下条件，AI 才能报告实现工作已经完成：

- 修改范围符合任务合同。
- 验收标准逐项得到验证。
- 必要测试已经运行并记录结果。
- 没有已知的致命错误。
- 没有未经说明的范围外改动。
- 相关文档已经同步更新。
- PR 包含风险、证据和回退说明。

AI 可以报告“实现完成并等待人工验收”，但不得代替项目负责人完成最终产品体验判断、合并或发布决定。

## 14. 最终汇报格式

完成工作后必须简要提供：

- 完成了什么。
- 修改了哪些模块。
- 执行了哪些测试以及结果。
- 尚存的风险或已知问题。
- 建议人工验证的步骤。
- PR 或相关 Linear 任务。

如果任务未完成，必须明确说明阻塞原因，不得把部分完成描述为全部完成。

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
