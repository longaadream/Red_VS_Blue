{
  "id": "aizen-black-coffin",
  "name": "黑棺",
  "description": "灵压。对本棋子5格内1个敌方棋子造成等同于本棋子攻击力150%的法术伤害，并使其获得定身，持续1回合。目标的下一个回合结束时，再次对其造成等量伤害。",
  "keywords": [
    "灵压",
    "定身"
  ],
  "statusTag": {
    "id": "spiritual-pressure",
    "type": "skill-rule",
    "name": "灵压",
    "rule": "rule-spiritual-pressure"
  },
  "targetText": "5格内1个敌方棋子",
  "effectTags": [
    "魔法伤害",
    "定身",
    "延迟伤害"
  ],
  "icon": "⬛",
  "kind": "active",
  "type": "super",
  "cooldownTurns": 2,
  "maxCharges": 0,
  "chargeCost": 2,
  "powerMultiplier": 1.5,
  "actionPointCost": 2,
  "range": "single",
  "requiresTarget": true,
  "targeting": {
    "steps": [
      {
        "kind": "target",
        "type": "piece",
        "filter": "enemy",
        "range": 5
      }
    ]
  },
  "code": "function executeSkill(context) { var caster = context.piece; var target = selectTarget({ type: 'piece', range: 5, filter: 'enemy' }); if (!target || target.needsTargetSelection) return target; var damage = Math.floor(caster.attack * 1.5); var result = dealDamage(caster, target, damage, 'magical', battle, 'aizen-black-coffin'); var living = battle.pieces.find(function(piece) { return piece.instanceId === target.instanceId && piece.currentHp > 0; }); if (living) { addStatusEffectById(living.instanceId, { id: 'aizen-black-coffin-' + caster.instanceId + '-' + living.instanceId, type: 'aizen-black-coffin', name: '黑棺', visible: true, currentDuration: 1, currentUses: 1, stacks: 1, sourceId: caster.instanceId, delayedDamage: damage, relatedRules: ['rule-aizen-black-coffin-end'] }); addStatusEffectById(living.instanceId, { id: 'black-coffin-root-' + caster.instanceId + '-' + living.instanceId, type: 'root', name: '定身', currentDuration: 1 }); addRuleById(living.instanceId, 'rule-aizen-black-coffin-end'); } return { success: true, message: caster.name + '以黑棺造成' + (result.damage || 0) + '点魔法伤害' }; }"
}
