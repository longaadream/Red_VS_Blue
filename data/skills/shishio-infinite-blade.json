{
  "id": "shishio-infinite-blade",
  "name": "无限刃",
  "description": "选择敌人（2），造成攻击力的真实伤害。结算后可使自身受到1点真实伤害并再次发动，不额外消耗行动点。每击杀1个棋子倍率升一级：1x／1.5x／2x。",
  "keywords": [
    "真实伤害"
  ],
  "targetText": "敌人（2）",
  "effectTags": [
    "damage",
    "recast",
    "growth"
  ],
  "icon": "⚔️",
  "kind": "active",
  "type": "normal",
  "cooldownTurns": 1,
  "actionPointCost": 1,
  "requiresTarget": true,
  "maxCharges": 0,
  "powerMultiplier": 1,
  "code": "function executeSkill(context) { var source = context.piece; var killTag = source.statusTags && source.statusTags.find(function(tag) { return tag.type === 'shishio-kills'; }); var kills = killTag ? (killTag.intensity || 0) : 0; var multiplier = kills >= 2 ? 2 : (kills >= 1 ? 1.5 : 1); var target = selectTarget({ type: 'piece', range: 2, filter: 'enemy' }); if (!target || target.needsTargetSelection) return target; var damage = Math.floor(source.attack * multiplier); var first = dealDamage(source, target, damage, 'true', battle, context.skill.id); return { success: true, message: source.name + '发动无限刃，对' + target.name + '造成' + (first.damage !== undefined ? first.damage : damage) + '点真实伤害' }; }",
  "previewCode": "function calculatePreview(piece) { var tag = piece.statusTags && piece.statusTags.find(function(item) { return item.type === 'shishio-kills'; }); var kills = tag ? (tag.intensity || 0) : 0; var multiplier = kills >= 2 ? 2 : (kills >= 1 ? 1.5 : 1); var damage = Math.floor((piece.attack || 0) * multiplier); return { description: \"选择敌人（2），造成攻击力的真实伤害。结算后可使自身受到1点真实伤害并再次发动，不额外消耗行动点。每击杀1个棋子倍率升一级：1x／1.5x／2x。\", expectedValues: { damage: damage, recastActionPointCost: 0 } }; }"
}
