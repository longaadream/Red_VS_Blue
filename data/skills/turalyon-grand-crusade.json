{
  "id": "turalyon-grand-crusade",
  "name": "圣光大远征",
  "description": "选择1至3个友方核心棋子，再选择6格内的1个空集结点，将其移至集结点及其相邻可落脚空格，并使其获得【圣盾】。",
  "keywords": ["位移", "圣盾"],
  "targetText": "1至3个友方核心棋子，然后选择6格内集结点",
  "effectTags": ["位移", "圣盾"],
  "icon": "🏳️",
  "kind": "active",
  "type": "super",
  "cooldownTurns": 3,
  "maxCharges": 1,
  "chargeCost": 1,
  "powerMultiplier": 1,
  "actionPointCost": 4,
  "targeting": { "steps": [] },
  "rollbackPendingTargetOnCancel": true,
  "code": "function executeSkill(context) {\n  var caster = context.piece;\n  var allies = battle.pieces.filter(function(piece) { return piece.currentHp > 0 && piece.ownerPlayerId === caster.ownerPlayerId && piece.isCore === true; }).sort(function(a, b) { return a.instanceId < b.instanceId ? -1 : (a.instanceId > b.instanceId ? 1 : 0); });\n  if (allies.length === 0) return { success: false, message: '没有可选择的友方核心棋子' };\n  function chooseGathering(ctx) {\n    var stageBattle = ctx.battle;\n    var sourceId = ctx.payload && ctx.payload.sourcePieceId;\n    var source = stageBattle.pieces.find(function(piece) { return piece.instanceId === sourceId && piece.currentHp > 0; });\n    var selectedIds = (ctx.pending && ctx.pending.selectedTargets || []).filter(function(target) { return target && target.type === 'piece'; }).map(function(target) { return target.pieceId; }).sort();\n    if (!source || selectedIds.length < 1 || selectedIds.length > 3 || new Set(selectedIds).size !== selectedIds.length) return { success: false, message: '选择的友方核心棋子无效' };\n    var selectedPieces = selectedIds.map(function(id) { return stageBattle.pieces.find(function(piece) { return piece.instanceId === id && piece.currentHp > 0 && piece.ownerPlayerId === source.ownerPlayerId && piece.isCore === true; }); });\n    if (selectedPieces.some(function(piece) { return !piece; })) return { success: false, message: '选中的友方核心棋子已失效' };\n    function livingAt(x, y) { return stageBattle.pieces.find(function(piece) { return piece.currentHp > 0 && piece.x === x && piece.y === y; }); }\n    function tileAt(x, y) { return stageBattle.map.tiles.find(function(tile) { return tile.x === x && tile.y === y; }); }\n    function slotsFor(x, y) { var offsets = [[0, 0], [0, -1], [-1, 0], [1, 0], [0, 1]]; var slots = []; for (var i = 0; i < offsets.length; i++) { var sx = x + offsets[i][0]; var sy = y + offsets[i][1]; var tile = tileAt(sx, sy); if (!tile || !tile.props || !tile.props.walkable) continue; var occupant = livingAt(sx, sy); if (occupant && selectedIds.indexOf(occupant.instanceId) === -1) continue; slots.push({ x: sx, y: sy }); } return slots; }\n    var gatheringCells = stageBattle.map.tiles.filter(function(tile) { return tile.props && tile.props.walkable && !livingAt(tile.x, tile.y) && Math.abs(tile.x - source.x) + Math.abs(tile.y - source.y) <= 6 && slotsFor(tile.x, tile.y).length >= selectedIds.length; }).sort(function(a, b) { if (a.y !== b.y) return a.y - b.y; return a.x - b.x; }).map(function(tile) { return { type: 'cell', x: tile.x, y: tile.y }; });\n    function completeGathering(finalCtx) {\n      var finalBattle = finalCtx.battle; var payload = finalCtx.payload || {}; var ids = (payload.selectedIds || []).slice().sort(); var finalSource = finalBattle.pieces.find(function(piece) { return piece.instanceId === payload.sourcePieceId && piece.currentHp > 0; });\n      if (!finalSource || ids.length < 1 || ids.length > 3 || new Set(ids).size !== ids.length) return { success: false, message: '圣光大远征的棋子选择已失效' };\n      var pieces = ids.map(function(id) { return finalBattle.pieces.find(function(piece) { return piece.instanceId === id && piece.currentHp > 0 && piece.ownerPlayerId === finalSource.ownerPlayerId && piece.isCore === true; }); });\n      if (pieces.some(function(piece) { return !piece; })) return { success: false, message: '圣光大远征的友方目标已失效' };\n      function finalLivingAt(x, y) { return finalBattle.pieces.find(function(piece) { return piece.currentHp > 0 && piece.x === x && piece.y === y; }); }\n      function finalTileAt(x, y) { return finalBattle.map.tiles.find(function(tile) { return tile.x === x && tile.y === y; }); }\n      var x = finalCtx.targetX; var y = finalCtx.targetY; var center = finalTileAt(x, y); if (!center || !center.props || !center.props.walkable || finalLivingAt(x, y) || Math.abs(x - finalSource.x) + Math.abs(y - finalSource.y) > 6) return { success: false, message: '圣光大远征的集结点已失效' };\n      var offsets = [[0, 0], [0, -1], [-1, 0], [1, 0], [0, 1]]; var slots = []; for (var i = 0; i < offsets.length; i++) { var sx = x + offsets[i][0]; var sy = y + offsets[i][1]; var tile = finalTileAt(sx, sy); if (!tile || !tile.props || !tile.props.walkable) continue; var occupant = finalLivingAt(sx, sy); if (occupant && ids.indexOf(occupant.instanceId) === -1) continue; slots.push({ x: sx, y: sy }); }\n      if (slots.length < ids.length) return { success: false, message: '集结点周围没有足够的可落脚格' };\n      for (var pi = 0; pi < pieces.length; pi++) { var piece = pieces[pi]; piece.x = slots[pi].x; piece.y = slots[pi].y; if (!piece.statusTags) piece.statusTags = []; var shield = piece.statusTags.find(function(tag) { return tag.type === 'divine-shield'; }); if (!shield) { piece.statusTags.push({ id: 'turalyon-grand-crusade-shield-' + piece.instanceId, name: '圣盾', type: 'divine-shield', currentDuration: -1, currentUses: -1, intensity: 1, stacks: 1, relatedRules: ['rule-divine-shield'] }); if (!piece.rules) piece.rules = []; if (!piece.rules.find(function(rule) { return rule.id === 'rule-divine-shield'; })) piece.rules.push({ id: 'rule-divine-shield' }); } }\n      return { success: true, message: finalSource.name + '发起圣光大远征，集结了' + pieces.length + '个友方并赋予圣盾' };\n    }\n    return { success: true, needsTargetSelection: true, playerId: ctx.playerId, title: '圣光大远征：选择集结点', targetType: 'grid', range: 6, filter: 'all', targetCandidates: gatheringCells, selectionMode: 'single', minSelections: 1, maxSelections: 1, canCancel: true, effectCode: completeGathering.toString(), payload: { sourcePieceId: sourceId, selectedIds: selectedIds } };\n  }\n  return { success: true, message: '', pendingTargetSelection: { playerId: caster.ownerPlayerId, title: '圣光大远征：选择1至3个友方核心棋子', targetType: 'piece', range: 99, filter: 'ally', targetCandidates: allies.map(function(piece) { return { type: 'piece', pieceId: piece.instanceId }; }), selectionMode: 'multi', minSelections: 1, maxSelections: 3, canCancel: true, effectCode: chooseGathering.toString(), payload: { sourcePieceId: caster.instanceId } } };\n}",
  "previewCode": "function calculatePreview(piece, skillDef) { return { description: skillDef.description, expectedValues: {\"maxTargets\":3,\"range\":6} }; }",
  "range": "area"
}
