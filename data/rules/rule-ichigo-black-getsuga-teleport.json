{
  "id": "rule-ichigo-black-getsuga-teleport",
  "name": "黑色月牙天冲·传送",
  "description": "黑色月牙天冲命中后，可选择首个目标相邻的一个合法空格传送，并结算落点上的天照、燃地与致命毒素。",
  "trigger": {
    "type": "afterSkillUsed"
  },
  "skillCode": "var source = context.rulePiece; if (!source || !context.sourcePiece || context.sourcePiece.instanceId !== source.instanceId || context.skillId !== 'ichigo-black-getsuga-tensho') return { success: false, message: '' }; var markerStore = battle.extensions && battle.extensions.ichigoBlackGetsugaTeleportByCaster; var candidates = markerStore && markerStore[source.instanceId]; if (!Array.isArray(candidates) || candidates.length === 0) return { success: false, message: '' }; var clearMarker = function() { delete markerStore[source.instanceId]; if (Object.keys(markerStore).length === 0 && battle.extensions) delete battle.extensions.ichigoBlackGetsugaTeleportByCaster; }; if (context.cancelled) { clearMarker(); return { success: false, message: '' }; } var targetPos = context.targetPosition || (context.targetX !== undefined && context.targetY !== undefined ? { x: context.targetX, y: context.targetY } : null); if (!targetPos) return { needsTargetSelection: true, playerId: source.ownerPlayerId, title: '选择黑色月牙天冲的传送落点', targetType: 'cell', targetCandidates: candidates, selectionMode: 'single', minSelections: 1, maxSelections: 1, canCancel: true, resumeOnCancel: true }; var isCandidate = candidates.some(function(cell) { return cell.x === targetPos.x && cell.y === targetPos.y; }); var tile = battle.map.tiles.find(function(item) { return item.x === targetPos.x && item.y === targetPos.y; }); var occupied = battle.pieces.some(function(piece) { return piece.currentHp > 0 && piece.x === targetPos.x && piece.y === targetPos.y; }); if (!isCandidate || !tile || !tile.props || !tile.props.walkable || occupied || source.currentHp <= 0 || !battle.pieces.some(function(piece) { return piece.instanceId === source.instanceId; })) { clearMarker(); return { success: false, message: '黑色月牙天冲的传送落点已失效' }; } clearMarker(); source.x = targetPos.x; source.y = targetPos.y; var landingMessages = []; var amaterasuCells = battle.extensions && battle.extensions.amaterasuCells; if (amaterasuCells && amaterasuCells.some(function(cell) { return cell.x === targetPos.x && cell.y === targetPos.y; })) { if (!source.statusTags) source.statusTags = []; var burnTag = source.statusTags.find(function(tag) { return tag.type === 'amaterasu-burn'; }); if (burnTag) burnTag.stacks = (burnTag.stacks || 1) + 1; else source.statusTags.push({ id: 'amaterasu-burn-' + source.instanceId, name: '天照灼烧', type: 'amaterasu-burn', currentDuration: -1, currentUses: -1, remainingDuration: -1, remainingUses: -1, intensity: 2, stacks: 1, relatedRules: [] }); landingMessages.push(source.name + '落在天照地格并获得1层天照'); } var burnTiles = battle.extensions && battle.extensions.shishioBurnTiles; if (source.currentHp > 0 && burnTiles && burnTiles.some(function(cell) { return cell.x === targetPos.x && cell.y === targetPos.y; })) { var shishio = battle.pieces.find(function(piece) { return piece.templateId === 'red-shishio' && piece.currentHp > 0; }); if (shishio) { var burnDamage = Math.ceil(source.currentHp * 0.5); var burnResult = dealDamage(shishio, source, burnDamage, 'true', battle, 'shishio-burn-move'); landingMessages.push(source.name + '落入燃地并受到' + (burnResult.damage !== undefined ? burnResult.damage : 0) + '点真实伤害'); } } if (source.currentHp > 0 && battle.players) { for (var pi = 0; pi < battle.players.length; pi++) { var trapOwner = battle.players[pi]; if (trapOwner.playerId === source.ownerPlayerId || !trapOwner.statusTags) continue; var toxinTag = trapOwner.statusTags.find(function(tag) { return tag.type === 'lethal-toxin' && tag.value === targetPos.x && tag.extraValue === targetPos.y; }); if (!toxinTag) continue; var trapCaster = battle.pieces.find(function(piece) { return piece.instanceId === toxinTag.sourceId && piece.currentHp > 0; }) || battle.pieces.find(function(piece) { return piece.ownerPlayerId === trapOwner.playerId && piece.currentHp > 0; }); var toxinDamage = toxinTag.intensity || 4; var toxinResult = dealDamage(trapCaster || source, source, toxinDamage, 'true', battle, 'lethal-toxin'); if (battle.extensions && battle.extensions.tileEffects) battle.extensions.tileEffects = battle.extensions.tileEffects.filter(function(effect) { return !(effect.tileType === 'lethal-toxin' && effect.sourceId === toxinTag.id); }); trapOwner.statusTags = trapOwner.statusTags.filter(function(tag) { return tag.id !== toxinTag.id; }); if (trapOwner.rules && !trapOwner.statusTags.some(function(tag) { return tag.type === 'lethal-toxin'; })) trapOwner.rules = trapOwner.rules.filter(function(rule) { return rule.id !== 'rule-blackwidow-toxin-player'; }); landingMessages.push(source.name + '触发致命毒素并受到' + (toxinResult.damage !== undefined ? toxinResult.damage : 0) + '点真实伤害'); if (source.currentHp <= 0) break; } } return { success: true, message: source.name + '传送至首个目标身旁' + (landingMessages.length ? '；' + landingMessages.join('；') : '') };"
}
