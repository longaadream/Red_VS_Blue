{
  "id": "rule-aizen-kyoka-rewrite",
  "name": "镜花水月·目标改写",
  "description": "镜花水月的秘密目标被单体技能指定时，可改写技能目标。",
  "trigger": { "type": "beforeSkillUse" },
  "skillCode": "var aizen = context.rulePiece; if (!aizen || aizen.currentHp <= 0) return { success: false, message: '' }; var source = context.sourcePiece; if (!source || source.ownerPlayerId === aizen.ownerPlayerId) return { success: false, message: '' }; var secret = aizen.statusTags && aizen.statusTags.find(function(tag) { return tag.type === 'aizen-kyoka-secret'; }); if (!secret || secret.opponentPlayerId !== source.ownerPlayerId) return { success: false, message: '' }; if (context.cancelled) return { success: false, message: '' }; var selected = Array.isArray(context.selectedTargets) ? context.selectedTargets : []; if (selected.length > 0) { var ref = selected[0]; var replacement = ref && ref.type === 'piece' ? battle.pieces.find(function(piece) { return piece.instanceId === ref.pieceId && piece.currentHp > 0; }) : null; if (!replacement || replacement.ownerPlayerId === aizen.ownerPlayerId || Math.abs(replacement.x - aizen.x) + Math.abs(replacement.y - aizen.y) > 3) return { success: false, message: '镜花水月的替代目标已失效' }; aizen.statusTags = (aizen.statusTags || []).filter(function(tag) { return tag.type !== 'aizen-kyoka-active' && tag.type !== 'aizen-kyoka-secret'; }); return { success: true, targetReplacementPieceId: replacement.instanceId, message: aizen.name + '发动镜花水月，改写了技能目标' }; } var original = context.targetPiece; if (!original || original.instanceId !== secret.targetPieceId) return { success: false, message: '' }; if (context.isSinglePieceTargetAction !== true) return { success: false, message: '' }; var candidates = battle.pieces.filter(function(piece) { return piece.currentHp > 0 && piece.ownerPlayerId !== aizen.ownerPlayerId && Math.abs(piece.x - aizen.x) + Math.abs(piece.y - aizen.y) <= 3; }).sort(function(left, right) { return left.instanceId < right.instanceId ? -1 : left.instanceId > right.instanceId ? 1 : 0; }).map(function(piece) { return { type: 'piece', pieceId: piece.instanceId }; }); if (!candidates.length) return { success: false, message: '' }; return { success: false, needsTargetSelection: true, playerId: aizen.ownerPlayerId, title: '镜花水月：选择替代目标', targetType: 'piece', filter: 'enemy', targetCandidates: candidates, selectionMode: 'single', minSelections: 1, maxSelections: 1, canCancel: true, resumeOnCancel: true };"
}
